# Writer Workbench Pingpong

Updated: 2026-06-23
Repository: WinterConstellation/writer-workbench

## User Requirements

1. This is a writing program.
2. Writing work must stay light, especially when pasting large manuscripts.
3. Customization and extensibility are the main differentiator.
4. API integration is a future design target.
5. Check similar external projects first, then choose the project direction, then continue detailed design.

## Product Identity

Writer Workbench is a Windows native long-form writing cockpit.

The manuscript input loop is the highest-priority path. Writing, paste, cursor movement, undo, save state, and simple visible metrics must remain responsive before any preview, analysis, export, visualization, or API feature receives work.

The app should not be shaped as a Scrivener clone, Markdown note app, or AI chat wrapper. It should be a local manuscript workbench where the user can assign commands to shortcuts, toolbar slots, context menus, panels, workspaces, and later macro/API workflows.

## External Reference Map

- Scrivener: reference for binder, scene/project organization, metadata, snapshots, split workflow, and export. Adopt project organization patterns, but keep the new app lighter and more command-customizable.
- novelWriter: reference for small document units, human-readable manuscript files, and side metadata. This strongly supports a plain-text canonical manuscript body with JSON sidecar metadata.
- Manuskript: reference for plot, character, world, premise, and outline panels. These should live beside the manuscript, not inside the editor input loop.
- Obsidian: reference for command palette, plugin extensibility, workspace behavior, and user-configurable commands. The app should treat commands as first-class extension points.
- Zettlr: reference for file-based writing workbench behavior, export pipeline, custom commands, templates, and writing statistics. Export should be a separate pipeline from editing.
- AvalonEdit: WPF editor candidate for large text and Korean IME stress tests. It should be evaluated against WPF TextBox before committing to a long-term editor surface.

## Direction Decision

Keep the Windows native WPF/.NET direction.

Reconsider the current `*.wwdoc.json` canonical manuscript source. For a writing program, the manuscript body should be human-readable and recoverable outside the app.

Preferred storage direction:

```text
project.writerproj/
  project.manifest.json
  binder.json
  commands.json
  shortcuts.json
  workspace.presets.json

  documents/
    scene-0001.wwtxt
    scene-0001.meta.json
    scene-0002.wwtxt
    scene-0002.meta.json

  index/
    project.sqlite

  snapshots/
  exports/
  plugins/
  api-profiles/
```

Canonical manuscript body:

```text
documents/*.wwtxt
```

Canonical scene metadata:

```text
documents/*.meta.json
```

Working index and cache:

```text
index/project.sqlite
```

SQLite should support fast search, counts, recent lists, tags, and cached derived data. The manuscript must remain recoverable from visible files without SQLite.

## Operating Loops

### Editor Input Loop

Responsibilities:

- Accept typing and paste.
- Update dirty state.
- Record last edit timestamp.
- Update only cheap visible metrics.
- Keep Korean IME composition responsive.

This loop is the royal road. Preview, indexing, export, API, visualization, and analysis enter through separate paths.

### Save Loop

Responsibilities:

- Save current scene body.
- Save scene metadata.
- Optionally create a snapshot.
- Queue background index refresh.
- Report manuscript save success separately from index refresh success.

A successful body/meta save means the manuscript is safe. Index refresh failure should be reported as a recoverable index problem.

### Index Loop

Responsibilities:

- Run in the background.
- Update SQLite search and statistics.
- Rebuild from manuscript files when needed.
- Avoid blocking the editor.

### Preview Loop

Preview is snapshot-based.

Triggers:

- User opens preview by button or shortcut.
- Save completes and preview surface is visible.
- Short document enters idle debounce, only if performance budget allows it.

Large document behavior:

- Render a limited chunk.
- Show preview timestamp and render limit.
- Offer explicit full preview generation as a separate operation.

Preferred naming:

```text
Snapshot Preview
```

Avoid treating preview as a live dependency of the editor input loop.

### Large Paste Loop

Large paste needs a dedicated path.

Flow:

1. Measure pasted text size.
2. If the text exceeds the configured threshold, stage it outside the active editor buffer.
3. Write staged content to a temp file or memory-mapped/staged buffer as appropriate.
4. Analyze possible split points by blank lines, headings, or custom rules.
5. Present commit choices:
   - insert into current scene,
   - split into new scenes,
   - store as import staging,
   - cancel.
6. Commit the selected result.
7. Queue background indexing.

Large paste is an import operation, not merely a keystroke event.

### API Loop

API integration should enter through command/plugin boundaries.

Input scopes:

- selected text,
- current scene,
- selected scenes,
- character card,
- plot card,
- project export snapshot.

Output modes:

- new note,
- suggestion patch,
- comparison view,
- export file,
- metadata update after user approval.

Preferred early structure:

```text
External command/plugin bridge
```

The app sends JSON input to a plugin or connector and receives JSON output. This keeps cloud API work outside the editor input loop and allows local tools, cloud APIs, and future model providers to share one integration pattern.

## Customization Spine

Every user-facing action should be a command.

Command shape:

```json
{
  "id": "manuscript.splitByBlankLines",
  "name": "Split scene by blank lines",
  "category": "Manuscript",
  "scope": "Editor",
  "parametersSchema": {
    "minChunkCharacters": 1000
  },
  "surfaces": ["shortcut", "toolbar", "contextMenu", "commandPalette", "workspacePreset", "macro"]
}
```

User customization files:

```text
shortcuts.json
toolbar.layout.json
context-menu.layout.json
workspace.presets.json
command-palette.favorites.json
macros.json
```

The UI should be a command placement surface. This keeps the app extensible without turning the main window into a drawer full of buttons.

## Immediate Review Notes For Current Repo

1. Keep WPF/.NET as the implementation direction.
2. Rework the canonical manuscript storage discussion before the file model hardens.
3. Split MainWindow responsibilities before adding more features.
4. Add explicit save-result separation: manuscript safe vs index refreshed.
5. Add closing behavior for dirty documents.
6. Add detached-window conflict policy.
7. Add large paste staging design before expanding editor features.
8. Add editor performance test plan: WPF TextBox vs AvalonEdit, Korean IME, large paste, undo, autosave under load.
9. Keep preview snapshot-based.
10. Put API behind command/plugin boundaries from the beginning.

## Next Design Task

Rewrite the design document around five paths:

1. Editor input path.
2. Save/recovery path.
3. Index/search/statistics path.
4. Preview/export path.
5. Command/plugin/API path.

Each path must specify:

- trigger,
- input,
- work performed,
- performance budget,
- failure state,
- visible user status,
- background behavior.

This is the next useful step before additional implementation.


---

# Current Implementation Snapshot

The section below was merged from the local WPF implementation handoff after the in-app help and large-editor safeguards were added. It preserves concrete implementation, verification, and next-step notes.

# Writer Workbench pingpong

Last updated: 2026-06-23 KST
Project root: C:\Users\Masters\Documents\글
Public snapshot: https://github.com/WinterConstellation/writer-workbench

## Current product frame

This is a Windows-only native writing workbench, not a Scrivener clone and not a Tauri/HTML app.

Core idea:
- Binder, outline, manuscript editing, search, autosave, and export are baseline furniture.
- The differentiator is a command-based custom writing cockpit.
- Important actions should become commands that can later be assigned to shortcuts, toolbar slots, panels, context menus, workspace presets, focus mode, and macro sequences.

Canonical direction:
- UI stack: WPF / .NET 8 / C#.
- Local-first and offline by default.
- Default project storage under C:\WriterWorkbench\Projects.
- Canonical manuscript file is structured JSON: *.wwdoc.json.
- TXT export is derived automatically for recovery and external use.
- SQLite is an index/settings store, not the only source of truth.
- AI/API/Codex/browser automation are excluded from MVP unless explicitly reopened.

## Implemented so far

### App skeleton and packaging

- WPF app project exists at src\WriterWorkbench.
- Test project exists at tests\WriterWorkbench.Tests.
- Solution file exists: WriterWorkbench.sln.
- Public GitHub repo was created and pushed from a clean snapshot:
  https://github.com/WinterConstellation/writer-workbench
- The public repo is a clean snapshot, not the old local history.
- Old Tauri-era docs still exist locally under docs\superpowers\*2026-06-22* but are superseded by the WPF docs from 2026-06-23.

### Project and storage model

- Project folder creation is implemented.
- Project manifest model exists.
- Document save/load is implemented.
- Canonical document JSON and derived TXT export are written.
- SQLite project index exists for metadata/search.
- Search path exists through the SQLite index.
- Starter/empty document repair exists.
- Document metrics exist: paragraphs, characters, plain text bytes.

### Document model and editor services

- WriterDocument and WriterParagraph models exist.
- Plain-text export service exists.
- Paragraph-preserving text merge service exists.
- Document edit session service exists.
- Large document editor view service exists.
- Large documents currently open as a bounded editable segment, default limit 5,000 characters.
- Hidden paragraphs are preserved when the visible segment is edited.

### Main UI

- Main screen is the workbench, not a landing page.
- UI includes:
  - binder list
  - title field
  - editor surface
  - search field and result list
  - metrics/status strips
  - operation progress panel
  - main dashboard surface
  - preview surface
- Preview is not continuously forced on screen. It opens by explicit preview toggle and switches surface.
- Main dashboard surface gives direct access to common actions.
- UI text in MainWindow.xaml is Korean.
- Shortcut settings, detached window, and focus exit dialog also have Korean UI text.

### Commands and shortcuts

- Command IDs exist in AppCommandIds.
- Command registry exists.
- Default command catalog and command slots exist.
- Shortcut manager exists.
- Shortcut conflict detection exists by scope.
- Shortcut profile persistence exists.
- Shortcut settings window exists.
- Ctrl+S manual save is wired through WPF command binding.

### Autosave and manual save

- Manual save is implemented.
- Autosave policy exists.
- Autosave has an idle delay policy.
- Overlapping autosaves are blocked.
- Autosave can be toggled from the UI.

### Focus session

- Focus session service exists.
- Default focus button uses 40 minutes.
- Early exit confirmation minimum is 20 characters by default.
- Focus exit dialog exists.
- This is app-level soft focus only.
- It does not change the Windows clock.
- It does not block Task Manager or OS security shortcuts.

### Workspace, windows, and session restore

- Workspace preset model exists.
- Preset slots 1, 2, 3 exist.
- Presets can be saved/applied.
- Startup preset selection exists.
- Window placement model exists.
- Detached document window exists.
- App session state service exists.
- Session restore tracks last project/document/surface/graphic preset.
- Single-instance guard exists.

### Graphic presets

Graphic preset catalog exists with:
- default
- dark
- comfort-1
- comfort-2
- comfort-3
- lavender

### Large operation feedback

- 15k stress document generator exists.
- Long operation progress tracker exists.
- Progress panel exists.
- ETA text exists.
- Remaining-time graph canvas/polyline exists.
- Preview update policy exists:
  - immediate for small text
  - debounced for medium text
  - paused for large text

## Verified recently

2026-06-23 KST update:
- P0 large-text input lag received a first direct fix.
- Main editor TextBox now uses large-text-friendly settings:
  - TextWrapping=NoWrap
  - UndoLimit=0
  - SpellCheck.IsEnabled=False
- Added regression/smoke tests:
  - MainEditorUsesLargeTextFriendlyInputSettings
  - WpfTextBoxAppendsToLargeNoWrapTextWithinSmokeThreshold
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 69 passed, 0 failed, 0 skipped.
- Important: this fixes the editor input surface first. If the user still sees freezing after pausing typing, inspect autosave/save/indexing next.

2026-06-23 KST help update:
- Added in-app Help.
- Entry points:
  - top workbench button: 도움말
  - main dashboard button: 도움말
  - default shortcut: F1
- Help content is stored in Core\Help\HelpCatalog.cs as item-by-item role text.
- Help window lists:
  - 구역
  - 항목
  - 역할
  - 사용
- Added tests:
  - HelpCatalogTests
  - HelpCommandTests
  - HelpWindowTests
- Cleaned AppCommandCatalog command names to normal Korean strings while adding the help command.
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 74 passed, 0 failed, 0 skipped.
  - dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-win-x64 --no-restore

2026-06-23 KST P0 input measurement update:
- Confirmed MainWindow editor input loop:
  - Test: MainEditorAcceptsLargeAppendWithinInputBudget
  - Measured run: 80,000 chars + append at end = 0.681 ms.
- Confirmed detached document window was missing large-input safeguards and was recomputing full metrics on every text change.
- Added detached-window regression tests:
  - DetachedEditorUsesLargeTextFriendlyInputSettings
  - DetachedLargeDocumentAcceptsSingleAppendWithinInputBudget
  - DetachedTextChangeDefersMetricsRefresh
- Detached measured run:
  - 15,000 paragraphs + append at end = 6.929 ms.
- Fix:
  - Detached TextBox now matches main editor large-text settings.
  - Detached metrics are delayed by 500 ms and no longer run synchronously in the keystroke path.
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 78 passed, 0 failed, 0 skipped.
- Not closed:
  - User's 30-second stress freeze still needs actual app create/load/save/render measurement.
  - Current large-document editor opens a bounded front segment; segment navigation/edit UX is still missing.

2026-06-23 KST large pipeline perf probe:
- Added reusable probe:
  - tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj
  - Run: dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release
- Latest measured run:
  - Date: 2026-06-23 23:47:35 KST
  - Temp project: C:\Users\Masters\AppData\Local\Temp\WriterWorkbenchPerfProbe\20260623-234733

| Area | Metric | ms |
|---|---:|---:|
| stress 15k 생성 | LargeDocumentFactory.Create | 40.761 |
| stress 15k 생성 | MainWindow stress command total | 242.179 |
| 문서 로드 | ProjectStore.LoadDocumentAsync | 92.971 |
| EditorBox 주입 | Create editor segment | 0.919 |
| EditorBox 주입 | MainWindow EditorBox segment set | 2.095 |
| EditorBox 주입 | Standalone full TextBox set baseline | 2.045 |
| WPF render/layout | MainWindow segment layout/render | 9.483 |
| WPF render/layout | Standalone full TextBox layout/render | 1.254 |
| 저장 하위 비용 | TXT export | 11.372 |
| 저장 하위 비용 | JSON serialize | 78.627 |
| 저장 하위 비용 | JSON atomic write | 58.601 |
| 저장 하위 비용 | TXT atomic write | 17.302 |
| 저장 하위 비용 | SQLite upsert | 114.381 |
| 저장 total | ProjectStore.SaveDocumentAsync total | 125.607 |
| preview/render 전환 | PreviewTextService.CreatePreview | 0.283 |
| preview/render 전환 | MainWindow preview switch/render | 2.587 |
| 분리창 동기화 | DetachedDocumentWindow construct | 56.779 |
| 분리창 동기화 | Detached append sync | 5.628 |
| 분리창 동기화 | Detached SaveAsync after edit | 206.731 |

Interpretation:
- The measured create/load/save/render paths do not reproduce a 30-second freeze.
- Current stress command total is about 242 ms in the offscreen probe.
- The cost centers are save/index paths, not the keystroke path:
  - SQLite upsert around 114 ms.
  - JSON serialize around 79 ms.
  - ProjectStore save around 126 ms.
  - Detached save after edit around 207 ms.
- Current MainWindow large-document edit path is still segment-based:
  - The visible editor segment is about 4,957 chars, not full 15k paragraphs.
  - Segment navigation/edit UX remains missing.
- Render/layout numbers are forced WPF offscreen layout/render passes. If the user still sees 30 seconds in the real app, next probe must attach to the running visible app/session or measure a launched exe end-to-end.

Last local verification:
- Command: dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
- Current result: 78 passed, 0 failed, 0 skipped.

Public repo verification:
- Repo: WinterConstellation/writer-workbench
- Visibility: public
- Branch: master
- Latest P0 input implementation commit: d2d3b3d

## Known problems / do not paper over

### P0: Large text input still lags

User reported that typing into a large file still lags.

2026-06-23 first fix:
- Editor input surface was changed from wrapping TextBox with undo history to NoWrap + UndoLimit=0 + spellcheck disabled.
- Tests now cover the intended input settings and a large no-wrap append smoke path.

Current mitigation is not enough:
- large document editor segment is capped around 5,000 chars
- preview update is throttled/paused
- autosave is debounced
- editor text wrapping is disabled for responsiveness

Next work should treat this as the top bug, not as solved.

2026-06-23 second P0 measurement/fix:
- Main editor input loop was measured directly:
  - Test: MainEditorAcceptsLargeAppendWithinInputBudget
  - Scenario: 80,000 characters in MainWindow EditorBox, append at the end.
  - Result: 0.681 ms in the measured run.
- Detached document window had a real synchronous large-input problem:
  - It used wrapping text input without the same large-text safeguards as the main editor.
  - It recomputed full document metrics on every text change.
  - Failing proof before fix: appending 9 chars immediately changed metrics from 1,755,000 to 1,755,009 chars.
- Fix applied:
  - Detached editor now uses NoWrap, UndoLimit=0, and spellcheck disabled.
  - Detached metrics refresh is delayed by a 500 ms dispatcher timer instead of running in the keystroke path.
  - Save still refreshes metrics immediately after persistence.
- Detached 15k input measurement:
  - Test: DetachedLargeDocumentAcceptsSingleAppendWithinInputBudget
  - Scenario: 15,000-paragraph document in DetachedDocumentWindow, append at the end.
  - Result: 6.929 ms in the measured run.

Important: this does not fully close the user's 30-second freeze report.
The measured keystroke path is now light, but the next P0 pass should measure actual app create/load/save/render flow, especially the stress 15k command and any real visible-window layout cost.

Likely next fixes:
- Measure stress 15k create/load/save/render flow in the running app.
- Keep expensive metrics/search/save/preview work out of every keystroke.
- Move expensive work behind timers/background tasks.
- Consider replacing WPF TextBox with AvalonEdit or a custom paragraph/segment editor.
- Add a visible-window typing-latency smoke test around large documents.
- Add explicit segment navigation/edit UX for large documents; current large-document editor opens a bounded front segment.

### P0: Original local workspace is not clean

The public GitHub repo was pushed from:
C:\Users\Masters\AppData\Local\Temp\writer-workbench-public-20260623213732

The active local workspace still has untracked/modified files.
Do not assume C:\Users\Masters\Documents\글 is already committed or synced.

Before future publish/sync work:
- check git status -sb
- decide whether to replace the local remote/history with the clean public repo or keep local history separate
- avoid accidentally publishing old Tauri-era history unless the user explicitly wants it

### P1: Old docs conflict with current direction

Old 2026-06-22 docs mention Tauri/React/WebView assumptions.
Current canonical docs are:
- docs\superpowers\specs\2026-06-23-writer-workbench-wpf-design.md
- docs\superpowers\plans\2026-06-23-writer-workbench-wpf-mvp-core.md

Future agents should read the 2026-06-23 docs first.

### P1: UI Korean must be verified in the running app

MainWindow.xaml has Korean strings, and tests pass.
Still verify in the actual app window after changes because console output can make encoding look broken.

### P1: MainWindow is too behavior-heavy

The app works, but MainWindow.xaml.cs currently owns too much behavior.
After P0 performance is fixed, move toward view-model/service boundaries carefully.

## Planned / next implementation

### Current user instruction / next handoff

The user asked to input the current direction into pingpong, not merely summarize it in chat.

Next agent should treat the following as active work order:

1. Actual running-app e2e performance measurement comes first.
   - The offscreen PerfProbe did not reproduce the 30-second freeze.
   - Do not mark the freeze solved from PerfProbe alone.
   - Measure the real executable path from user action to visible UI response.
   - Target flows:
     - Stress 15k button click to usable editor.
     - Large document load to usable editor.
     - Typing into the loaded stress document.
     - Save/autosave after edit.
     - Preview switch.
     - Detached window open, edit, save.

2. Large document editing UX is still incomplete.
   - Current main editor opens only a bounded front segment of about 4,957 chars.
   - This is a performance guard, not a complete large-manuscript editing design.
   - Add explicit segment navigation/edit workflow before claiming large document editing is usable.

3. Storage format conflict remains unresolved.
   - Current implementation uses `documents/*.wwdoc.json`.
   - Direction note recommends `documents/*.wwtxt` plus `documents/*.meta.json` and SQLite index/cache.
   - Do not harden more project-file behavior until this is decided or migration-safe.

4. Local/public repo alignment is still P0/P1.
   - Public repo is the clean pushed snapshot.
   - Local `C:\Users\Masters\Documents\글` still has untracked project files and old local history.
   - Do not assume local git state is publish-ready.
   - Check public temp repo and local workspace separately before commit/push.

5. Keep scope tight.
   - Do not reopen Tauri/WPF stack debate.
   - Do not add AI/API/Codex/Chrome integration while P0 editor/storage/repo issues are open.
   - Do not report "large text fixed" unless real visible-app measurement supports it.

2026-06-24 KST implementation note:
- User redirected work away from more performance digging:
  - "Do not dig performance; build features."
  - Only check whether the large-manuscript baseline is broken.
  - First feature area: binder scene management.
- Implemented first binder scene management pass:
  - Binder buttons for scene duplicate, delete, move up, move down.
  - ProjectStore scene operations:
    - DuplicateDocumentAsync
    - DeleteDocumentAsync
    - MoveDocumentAsync
  - Manifest order is now preserved instead of always sorting by scene id.
  - SaveDocumentAsync updates an existing manifest entry in place.
  - Delete removes wwdoc/json, derived txt, manifest entry, and SQLite index row.
  - Delete refuses to remove the last remaining scene.
  - AppCommandCatalog was normalized to usable Korean command labels while adding binder commands.
- Verification:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 83 passed, 0 failed, 0 skipped.
- Large manuscript baseline check only:
  - dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release
  - Latest run: 2026-06-24 01:10:38 KST
  - MainWindow stress command total: 201.346 ms.
  - ProjectStore.SaveDocumentAsync total: 120.252 ms.
  - MainWindow EditorBox segment set: 1.484 ms.
  - Detached append sync: 3.329 ms.
  - No new 30-second path was introduced by binder scene management.

### Immediate next work

1. Continue binder scene management UX from the first pass.
2. Add explicit large-document segment navigation/edit workflow.
3. Verify Korean UI in the actual running app.
4. Clean up local/public repo alignment.
5. Remove or archive superseded Tauri-era docs after user approval.

### MVP 0.1 remaining direction

- Make the editor stable under large manuscript workloads.
- Keep autosave reliable and visible.
- Keep TXT export automatic.
- Keep preview explicit and cheap.
- Keep command/shortcut system as first-class core.
- Improve workspace preset reliability for one monitor first.

### MVP 0.2 direction

- Better detached tab/window workflow.
- Multi-monitor preset restore with user-selected monitor assignment.
- Startup preset auto-apply polish.
- Better search UI.
- Session logs.
- Paragraph tags and review flags.

### MVP 0.3 direction

- Plot/person/reference panels.
- Cover layout tools.
- CSV/JSON data cleanup exports.
- Rule-based manuscript checker.
- Visualization.
- Spellcheck/proofreading strategy.

### Later, opt-in only

- Chrome launcher bridge.
- External API providers.
- Local algorithms.
- AI analysis.
- Codex integration only if it can be done without unsafe account/session scraping.

## Security and privacy notes

- Default app behavior should require no internet.
- Do not send manuscript text externally by default.
- Do not read Chrome cookies/passwords/session tokens.
- Do not store future API keys inside manuscript project folders.
- Do not add hard OS lockout behavior.
- Focus mode must remain app-level and user-controllable.

## Handoff rule

When resuming work, do not start with stack debate.
Use WPF/.NET unless the user explicitly reopens stack choice.

Completion upload rule:
- After every work batch, update pingpong with what changed, what was verified, and what remains.
- Upload the updated pingpong to the public repo before reporting completion.
- Upload related project data to the repo as well when the work produced or changed data files, measurement outputs, fixtures, docs, specs, exports, or other non-sensitive artifacts needed for handoff.
- If no separate data artifact was produced, say that explicitly in the final report.
- Do not leave the public repo behind local-only work unless the user explicitly says not to push.

First read:
1. this file
2. docs\superpowers\specs\2026-06-23-writer-workbench-wpf-design.md
3. docs\superpowers\plans\2026-06-23-writer-workbench-wpf-mvp-core.md

Then check:
1. git status -sb
2. dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
3. current large-text typing behavior

## 2026-06-24 KST feature implementation update: Binder scene management polish

- Implemented binder scene rename with `document.renameScene`, a binder toolbar `이름` button, and `SceneRenameDialog`.
- New scenes keep automatic sequential ids and immediately focus/select the title box so the title can be edited right away.
- Scene delete now asks for confirmation before deleting and keeps the next available scene selected after deletion.
- Scene duplicate, move up/down, create, delete, and rename status messages include the affected scene id and title.
- Binder order continues to persist through manifest order, and refresh remains manifest/metadata driven instead of loading document bodies.
- `ProjectStore.RenameDocumentAsync` updates the wwdoc metadata, manifest title, TXT export, and SQLite search title together.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/SceneRenameDialog.xaml`
- `src/WriterWorkbench/SceneRenameDialog.xaml.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/SceneRenameDialogTests.cs`

Tests added/expanded:
- create sequential scene ids and title metadata persistence.
- rename manifest/document/search-index synchronization.
- duplicate order persistence after reload.
- delete persistence after reload.
- reorder persistence after reload.
- manifest-only binder metadata load without document body files.
- command catalog and MainWindow smoke coverage for `document.renameScene`.
- scene rename dialog title behavior.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ProjectStoreTests|AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands|SceneRenameDialogTests"` -> 17 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 88 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj --no-restore` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 224.996 ms.
- ProjectStore.SaveDocumentAsync total: 128.336 ms.
- ProjectStore.LoadDocumentAsync: 119.756 ms.
- MainWindow EditorBox segment set: 1.850 ms.
- MainWindow preview switch/render: 1.731 ms.
- Detached append sync: 8.887 ms.

Remaining problems:
- Actual app UI should still be visually checked in a running WPF window after more Korean-label cleanup.
- Large-file typing lag remains a separate editor workflow task; this batch did not investigate performance beyond guardrail checks.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Scene Inspector MVP

- Implemented `SceneMetadata` sidecar storage under `documents/{scene-id}.meta.json`.
- Added `SceneStatus` values for Draft/Revising/Final/Excluded, shown in Korean as 초고/수정중/완료/제외.
- Added `SceneMetadataStore` for default metadata creation, load, save, and delete.
- Metadata JSON stores status as a readable string, for example `"Status": "Revising"`.
- Connected `ProjectStore` so new scenes get default metadata, duplicated scenes copy synopsis/status/tags/target count with a refreshed `UpdatedAt`, and deleted scenes remove metadata files.
- Added a right-panel Scene Inspector MVP below search results with 시놉시스, 상태, 태그, 목표 글자 수, 현재 글자 수, 수정 시각, and a metadata-only 저장 button.
- Active scene load refreshes Inspector metadata. Inspector save writes only `.meta.json`; `EditorBox_TextChanged` still only marks dirty/edit time and does not save metadata or rewrite body files.
- Current character count uses `DocumentMetricsService` during document load/save metric refresh, not on every keystroke.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadata.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadataStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/SceneMetadataStoreTests.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added/expanded:
- `SceneMetadataStoreTests.LoadCreatesDefaultMetadataWhenMissing`
- `SceneMetadataStoreTests.SavesAndLoadsSynopsisStatusTagsAndTargetCharacterCount`
- `SceneMetadataStoreTests.MetadataSaveDoesNotRewriteDocumentBodyFiles`
- `SceneMetadataStoreTests.DeleteRemovesMetadataFileWhenPresent`
- `ProjectStoreTests.CreateDocumentGeneratesSequentialSceneIdsAndStoresEditableTitle` now verifies default metadata.
- `ProjectStoreTests.DuplicateDocumentCopiesSceneAfterSource` now verifies metadata copy behavior.
- `ProjectStoreTests.DeleteDocumentRemovesFilesManifestEntryAndSearchIndex` now verifies metadata cleanup.
- `MainWindowSmokeTests.MainWindowContainsSceneInspectorSurface`

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneMetadataStoreTests|ProjectStoreTests|MainWindowContainsSceneInspectorSurface"` -> 18 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 93 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 233.815 ms.
- ProjectStore.SaveDocumentAsync total: 182.813 ms.
- ProjectStore.LoadDocumentAsync: 169.107 ms.
- MainWindow EditorBox segment set: 2.008 ms.
- MainWindow preview switch/render: 2.410 ms.
- Detached append sync: 5.305 ms.
- Detached SaveAsync after edit: 302.007 ms.
- Save path stayed under the user baseline of 500 ms in this probe.

Remaining problems:
- Inspector UI is MVP only; no tag autocomplete, search/inspector tab switch, or command-palette metadata command yet.
- Actual running WPF UI still needs visual inspection for right-panel spacing and Korean label fit.
- Excluded scenes are stored for future Export MVP, but export skipping is not implemented yet.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Export MVP

- Implemented explicit TXT export commands for the current scene and full manuscript.
- Added `exports/` as the export destination under each `.writerproj` project root.
- Current scene export writes only the active scene body to UTF-8 TXT.
- Full manuscript export follows binder/manifest order and joins included scenes into one UTF-8 TXT file.
- Full manuscript export skips scenes whose `SceneMetadata.Status` is `Excluded`.
- Export output path is shown in the status message after success.
- Export is connected only through explicit buttons/commands: `export.currentScene` and `export.fullManuscript`.
- `EditorBox_TextChanged` remains dirty/edit-time only; export is not connected to the input loop.
- Added read-only metadata loading through `SceneMetadataStore.LoadExistingOrDefaultAsync` so full export can inspect status without creating or rewriting metadata files.

Files changed:
- `src/WriterWorkbench/Core/Export/ManuscriptExportService.cs`
- `src/WriterWorkbench/Core/Export/ManuscriptExportResult.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadataStore.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/ManuscriptExportServiceTests.cs`
- `tests/WriterWorkbench.Tests/SceneMetadataStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added/expanded:
- current scene export writes UTF-8 TXT under `exports/`.
- full manuscript export preserves binder order.
- full manuscript export skips `SceneStatus.Excluded`.
- Korean body text is preserved through UTF-8 export.
- export creates the `exports/` directory when missing.
- no-included-scene and empty-manifest full export fail without writing a misleading output file.
- export does not mutate document JSON, sidecar TXT, or metadata JSON.
- full export does not create a missing metadata sidecar just to inspect status.
- repeated explicit exports create distinct TXT files instead of overwriting the prior export.
- command catalog and MainWindow smoke coverage for export commands/buttons.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExportFullManuscriptDoesNotCreateMissingMetadataFile"` -> 1 failed before the metadata read-only fix, then 1 passed after the fix.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExportCurrentSceneDoesNotOverwriteRepeatedExports"` -> 1 failed before unique export suffixes, then 1 passed after the fix.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ManuscriptExportServiceTests|SceneMetadataStoreTests|AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands"` -> 16 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ManuscriptExportServiceTests"` -> 8 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 102 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj --no-restore` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 386.512 ms.
- ProjectStore.SaveDocumentAsync total: 207.487 ms.
- ProjectStore.LoadDocumentAsync: 110.425 ms.
- MainWindow EditorBox segment set: 1.570 ms.
- MainWindow preview switch/render: 1.904 ms.
- Detached append sync: 7.561 ms.
- Export MVP was checked as an explicit command path only; no export work was added to typing/input events.

Remaining problems:
- Export MVP is TXT-only; no compile format profiles, headings, chapter separators, cover-layout export, or per-status export filters beyond `Excluded`.
- Actual running WPF UI still needs visual inspection for button placement and Korean label fit.
- No separate data artifact was produced; the perf result was not saved as a file.
