# Writer Workbench Pingpong

Updated: 2026-06-24
Repository: WinterConstellation/writer-workbench

## User Requirements

1. This is a writing program.
2. Writing work must stay light, especially when pasting large manuscripts.
3. Customization and extensibility are the main differentiator.
4. API integration is a future design target.
5. Check similar external projects first, then choose the project direction, then continue detailed design.
6. If a task creates reusable fixtures, sample project data, measurement output, exports, snapshots, or other handoff data, upload those artifacts to the public repo with the code.
7. If no separate data artifact is produced, state `No separate data artifact was produced` / `별도 데이터 산출물 없음` in the pingpong entry.

## Product Identity

Writer Workbench is a Windows native long-form writing cockpit.

The manuscript input loop is the highest-priority path. Writing, paste, cursor movement, undo, save state, and simple visible metrics must remain responsive before any preview, analysis, export, visualization, or API feature receives work.

The app should not be shaped as a Scrivener clone, Markdown note app, or AI chat wrapper. It should be a local manuscript workbench where the user can assign commands to shortcuts, toolbar slots, context menus, panels, workspaces, and later macro/API workflows.

## External Reference Map

- Scrivener: reference for binder, scene/project organization, metadata, snapshots, split workflow, and export. Adopt project organization patterns, but keep the new app lighter and more command-customizable.
- novelWriter: reference for small document units, human-readable manuscript files, and side metadata. This strongly supports a plain-text canonical manuscript body with JSON sidecar metadata.
- Manuskript: reference for plot, character, world, premise, and outline panels. These should live beside the manuscript, not inside the editor input loop.
- Obsidian: reference for command palette, plugin extensibility, workspace behavior, and user-configurable commands. The app should treat commands as first-class extension points.
- Zettlr: reference for file-based writing workbench behavior, export pipeline, custom commands, templates, and writing statistics. Export should be a separate pipeline from editing.
- AvalonEdit: WPF editor candidate for large text and Korean IME stress tests. It should be evaluated against WPF TextBox before committing to a long-term editor surface.

## Direction Decision

Keep the Windows native WPF/.NET direction.

Reconsider the current `*.wwdoc.json` canonical manuscript source. For a writing program, the manuscript body should be human-readable and recoverable outside the app.

Preferred storage direction:

```text
project.writerproj/
  project.manifest.json
  binder.json
  commands.json
  shortcuts.json
  workspace.presets.json

  documents/
    scene-0001.wwtxt
    scene-0001.meta.json
    scene-0002.wwtxt
    scene-0002.meta.json

  index/
    project.sqlite

  snapshots/
  exports/
  plugins/
  api-profiles/
```

Canonical manuscript body:

```text
documents/*.wwtxt
```

Canonical scene metadata:

```text
documents/*.meta.json
```

Working index and cache:

```text
index/project.sqlite
```

SQLite should support fast search, counts, recent lists, tags, and cached derived data. The manuscript must remain recoverable from visible files without SQLite.

## Operating Loops

### Editor Input Loop

Responsibilities:

- Accept typing and paste.
- Update dirty state.
- Record last edit timestamp.
- Update only cheap visible metrics.
- Keep Korean IME composition responsive.

This loop is the royal road. Preview, indexing, export, API, visualization, and analysis enter through separate paths.

### Save Loop

Responsibilities:

- Save current scene body.
- Save scene metadata.
- Optionally create a snapshot.
- Queue background index refresh.
- Report manuscript save success separately from index refresh success.

A successful body/meta save means the manuscript is safe. Index refresh failure should be reported as a recoverable index problem.

### Index Loop

Responsibilities:

- Run in the background.
- Update SQLite search and statistics.
- Rebuild from manuscript files when needed.
- Avoid blocking the editor.

### Preview Loop

Preview is snapshot-based.

Triggers:

- User opens preview by button or shortcut.
- Save completes and preview surface is visible.
- Short document enters idle debounce, only if performance budget allows it.

Large document behavior:

- Render a limited chunk.
- Show preview timestamp and render limit.
- Offer explicit full preview generation as a separate operation.

Preferred naming:

```text
Snapshot Preview
```

Avoid treating preview as a live dependency of the editor input loop.

### Large Paste Loop

Large paste needs a dedicated path.

Flow:

1. Measure pasted text size.
2. If the text exceeds the configured threshold, stage it outside the active editor buffer.
3. Write staged content to a temp file or memory-mapped/staged buffer as appropriate.
4. Analyze possible split points by blank lines, headings, or custom rules.
5. Present commit choices:
   - insert into current scene,
   - split into new scenes,
   - store as import staging,
   - cancel.
6. Commit the selected result.
7. Queue background indexing.

Large paste is an import operation, not merely a keystroke event.

### API Loop

API integration should enter through command/plugin boundaries.

Input scopes:

- selected text,
- current scene,
- selected scenes,
- character card,
- plot card,
- project export snapshot.

Output modes:

- new note,
- suggestion patch,
- comparison view,
- export file,
- metadata update after user approval.

Preferred early structure:

```text
External command/plugin bridge
```

The app sends JSON input to a plugin or connector and receives JSON output. This keeps cloud API work outside the editor input loop and allows local tools, cloud APIs, and future model providers to share one integration pattern.

## Customization Spine

Every user-facing action should be a command.

Command shape:

```json
{
  "id": "manuscript.splitByBlankLines",
  "name": "Split scene by blank lines",
  "category": "Manuscript",
  "scope": "Editor",
  "parametersSchema": {
    "minChunkCharacters": 1000
  },
  "surfaces": ["shortcut", "toolbar", "contextMenu", "commandPalette", "workspacePreset", "macro"]
}
```

User customization files:

```text
shortcuts.json
toolbar.layout.json
context-menu.layout.json
workspace.presets.json
command-palette.favorites.json
macros.json
```

The UI should be a command placement surface. This keeps the app extensible without turning the main window into a drawer full of buttons.

## Immediate Review Notes For Current Repo

1. Keep WPF/.NET as the implementation direction.
2. Rework the canonical manuscript storage discussion before the file model hardens.
3. Split MainWindow responsibilities before adding more features.
4. Add explicit save-result separation: manuscript safe vs index refreshed.
5. Add closing behavior for dirty documents.
6. Add detached-window conflict policy.
7. Add large paste staging design before expanding editor features.
8. Add editor performance test plan: WPF TextBox vs AvalonEdit, Korean IME, large paste, undo, autosave under load.
9. Keep preview snapshot-based.
10. Put API behind command/plugin boundaries from the beginning.

## Next Design Task

Rewrite the design document around five paths:

1. Editor input path.
2. Save/recovery path.
3. Index/search/statistics path.
4. Preview/export path.
5. Command/plugin/API path.

Each path must specify:

- trigger,
- input,
- work performed,
- performance budget,
- failure state,
- visible user status,
- background behavior.

This is the next useful step before additional implementation.


---

# Current Implementation Snapshot

The section below was merged from the local WPF implementation handoff after the in-app help and large-editor safeguards were added. It preserves concrete implementation, verification, and next-step notes.

# Writer Workbench pingpong

Last updated: 2026-06-23 KST
Project root: C:\Users\Masters\Documents\글
Public snapshot: https://github.com/WinterConstellation/writer-workbench

## Current product frame

This is a Windows-only native writing workbench, not a Scrivener clone and not a Tauri/HTML app.

Core idea:
- Binder, outline, manuscript editing, search, autosave, and export are baseline furniture.
- The differentiator is a command-based custom writing cockpit.
- Important actions should become commands that can later be assigned to shortcuts, toolbar slots, panels, context menus, workspace presets, focus mode, and macro sequences.

Canonical direction:
- UI stack: WPF / .NET 8 / C#.
- Local-first and offline by default.
- Default project storage under C:\WriterWorkbench\Projects.
- Canonical manuscript file is structured JSON: *.wwdoc.json.
- TXT export is derived automatically for recovery and external use.
- SQLite is an index/settings store, not the only source of truth.
- AI/API/Codex/browser automation are excluded from MVP unless explicitly reopened.

## Implemented so far

### App skeleton and packaging

- WPF app project exists at src\WriterWorkbench.
- Test project exists at tests\WriterWorkbench.Tests.
- Solution file exists: WriterWorkbench.sln.
- Public GitHub repo was created and pushed from a clean snapshot:
  https://github.com/WinterConstellation/writer-workbench
- The public repo is a clean snapshot, not the old local history.
- Old Tauri-era docs still exist locally under docs\superpowers\*2026-06-22* but are superseded by the WPF docs from 2026-06-23.

### Project and storage model

- Project folder creation is implemented.
- Project manifest model exists.
- Document save/load is implemented.
- Canonical document JSON and derived TXT export are written.
- SQLite project index exists for metadata/search.
- Search path exists through the SQLite index.
- Starter/empty document repair exists.
- Document metrics exist: paragraphs, characters, plain text bytes.

### Document model and editor services

- WriterDocument and WriterParagraph models exist.
- Plain-text export service exists.
- Paragraph-preserving text merge service exists.
- Document edit session service exists.
- Large document editor view service exists.
- Large documents currently open as a bounded editable segment, default limit 5,000 characters.
- Hidden paragraphs are preserved when the visible segment is edited.

### Main UI

- Main screen is the workbench, not a landing page.
- UI includes:
  - binder list
  - title field
  - editor surface
  - search field and result list
  - metrics/status strips
  - operation progress panel
  - main dashboard surface
  - preview surface
- Preview is not continuously forced on screen. It opens by explicit preview toggle and switches surface.
- Main dashboard surface gives direct access to common actions.
- UI text in MainWindow.xaml is Korean.
- Shortcut settings, detached window, and focus exit dialog also have Korean UI text.

### Commands and shortcuts

- Command IDs exist in AppCommandIds.
- Command registry exists.
- Default command catalog and command slots exist.
- Shortcut manager exists.
- Shortcut conflict detection exists by scope.
- Shortcut profile persistence exists.
- Shortcut settings window exists.
- Ctrl+S manual save is wired through WPF command binding.

### Autosave and manual save

- Manual save is implemented.
- Autosave policy exists.
- Autosave has an idle delay policy.
- Overlapping autosaves are blocked.
- Autosave can be toggled from the UI.

### Focus session

- Focus session service exists.
- Default focus button uses 40 minutes.
- Early exit confirmation minimum is 20 characters by default.
- Focus exit dialog exists.
- This is app-level soft focus only.
- It does not change the Windows clock.
- It does not block Task Manager or OS security shortcuts.

### Workspace, windows, and session restore

- Workspace preset model exists.
- Preset slots 1, 2, 3 exist.
- Presets can be saved/applied.
- Startup preset selection exists.
- Window placement model exists.
- Detached document window exists.
- App session state service exists.
- Session restore tracks last project/document/surface/graphic preset.
- Single-instance guard exists.

### Graphic presets

Graphic preset catalog exists with:
- default
- dark
- comfort-1
- comfort-2
- comfort-3
- lavender

### Large operation feedback

- 15k stress document generator exists.
- Long operation progress tracker exists.
- Progress panel exists.
- ETA text exists.
- Remaining-time graph canvas/polyline exists.
- Preview update policy exists:
  - immediate for small text
  - debounced for medium text
  - paused for large text

## Verified recently

2026-06-23 KST update:
- P0 large-text input lag received a first direct fix.
- Main editor TextBox now uses large-text-friendly settings:
  - TextWrapping=NoWrap
  - UndoLimit=0
  - SpellCheck.IsEnabled=False
- Added regression/smoke tests:
  - MainEditorUsesLargeTextFriendlyInputSettings
  - WpfTextBoxAppendsToLargeNoWrapTextWithinSmokeThreshold
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 69 passed, 0 failed, 0 skipped.
- Important: this fixes the editor input surface first. If the user still sees freezing after pausing typing, inspect autosave/save/indexing next.

2026-06-23 KST help update:
- Added in-app Help.
- Entry points:
  - top workbench button: 도움말
  - main dashboard button: 도움말
  - default shortcut: F1
- Help content is stored in Core\Help\HelpCatalog.cs as item-by-item role text.
- Help window lists:
  - 구역
  - 항목
  - 역할
  - 사용
- Added tests:
  - HelpCatalogTests
  - HelpCommandTests
  - HelpWindowTests
- Cleaned AppCommandCatalog command names to normal Korean strings while adding the help command.
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 74 passed, 0 failed, 0 skipped.
  - dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-win-x64 --no-restore

2026-06-23 KST P0 input measurement update:
- Confirmed MainWindow editor input loop:
  - Test: MainEditorAcceptsLargeAppendWithinInputBudget
  - Measured run: 80,000 chars + append at end = 0.681 ms.
- Confirmed detached document window was missing large-input safeguards and was recomputing full metrics on every text change.
- Added detached-window regression tests:
  - DetachedEditorUsesLargeTextFriendlyInputSettings
  - DetachedLargeDocumentAcceptsSingleAppendWithinInputBudget
  - DetachedTextChangeDefersMetricsRefresh
- Detached measured run:
  - 15,000 paragraphs + append at end = 6.929 ms.
- Fix:
  - Detached TextBox now matches main editor large-text settings.
  - Detached metrics are delayed by 500 ms and no longer run synchronously in the keystroke path.
- Verification after this change:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 78 passed, 0 failed, 0 skipped.
- Not closed:
  - User's 30-second stress freeze still needs actual app create/load/save/render measurement.
  - Current large-document editor opens a bounded front segment; segment navigation/edit UX is still missing.

2026-06-23 KST large pipeline perf probe:
- Added reusable probe:
  - tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj
  - Run: dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release
- Latest measured run:
  - Date: 2026-06-23 23:47:35 KST
  - Temp project: C:\Users\Masters\AppData\Local\Temp\WriterWorkbenchPerfProbe\20260623-234733

| Area | Metric | ms |
|---|---:|---:|
| stress 15k 생성 | LargeDocumentFactory.Create | 40.761 |
| stress 15k 생성 | MainWindow stress command total | 242.179 |
| 문서 로드 | ProjectStore.LoadDocumentAsync | 92.971 |
| EditorBox 주입 | Create editor segment | 0.919 |
| EditorBox 주입 | MainWindow EditorBox segment set | 2.095 |
| EditorBox 주입 | Standalone full TextBox set baseline | 2.045 |
| WPF render/layout | MainWindow segment layout/render | 9.483 |
| WPF render/layout | Standalone full TextBox layout/render | 1.254 |
| 저장 하위 비용 | TXT export | 11.372 |
| 저장 하위 비용 | JSON serialize | 78.627 |
| 저장 하위 비용 | JSON atomic write | 58.601 |
| 저장 하위 비용 | TXT atomic write | 17.302 |
| 저장 하위 비용 | SQLite upsert | 114.381 |
| 저장 total | ProjectStore.SaveDocumentAsync total | 125.607 |
| preview/render 전환 | PreviewTextService.CreatePreview | 0.283 |
| preview/render 전환 | MainWindow preview switch/render | 2.587 |
| 분리창 동기화 | DetachedDocumentWindow construct | 56.779 |
| 분리창 동기화 | Detached append sync | 5.628 |
| 분리창 동기화 | Detached SaveAsync after edit | 206.731 |

Interpretation:
- The measured create/load/save/render paths do not reproduce a 30-second freeze.
- Current stress command total is about 242 ms in the offscreen probe.
- The cost centers are save/index paths, not the keystroke path:
  - SQLite upsert around 114 ms.
  - JSON serialize around 79 ms.
  - ProjectStore save around 126 ms.
  - Detached save after edit around 207 ms.
- Current MainWindow large-document edit path is still segment-based:
  - The visible editor segment is about 4,957 chars, not full 15k paragraphs.
  - Segment navigation/edit UX remains missing.
- Render/layout numbers are forced WPF offscreen layout/render passes. If the user still sees 30 seconds in the real app, next probe must attach to the running visible app/session or measure a launched exe end-to-end.

Last local verification:
- Command: dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
- Current result: 78 passed, 0 failed, 0 skipped.

Public repo verification:
- Repo: WinterConstellation/writer-workbench
- Visibility: public
- Branch: master
- Latest P0 input implementation commit: d2d3b3d

## Known problems / do not paper over

### P0: Large text input still lags

User reported that typing into a large file still lags.

2026-06-23 first fix:
- Editor input surface was changed from wrapping TextBox with undo history to NoWrap + UndoLimit=0 + spellcheck disabled.
- Tests now cover the intended input settings and a large no-wrap append smoke path.

Current mitigation is not enough:
- large document editor segment is capped around 5,000 chars
- preview update is throttled/paused
- autosave is debounced
- editor text wrapping is disabled for responsiveness

Next work should treat this as the top bug, not as solved.

2026-06-23 second P0 measurement/fix:
- Main editor input loop was measured directly:
  - Test: MainEditorAcceptsLargeAppendWithinInputBudget
  - Scenario: 80,000 characters in MainWindow EditorBox, append at the end.
  - Result: 0.681 ms in the measured run.
- Detached document window had a real synchronous large-input problem:
  - It used wrapping text input without the same large-text safeguards as the main editor.
  - It recomputed full document metrics on every text change.
  - Failing proof before fix: appending 9 chars immediately changed metrics from 1,755,000 to 1,755,009 chars.
- Fix applied:
  - Detached editor now uses NoWrap, UndoLimit=0, and spellcheck disabled.
  - Detached metrics refresh is delayed by a 500 ms dispatcher timer instead of running in the keystroke path.
  - Save still refreshes metrics immediately after persistence.
- Detached 15k input measurement:
  - Test: DetachedLargeDocumentAcceptsSingleAppendWithinInputBudget
  - Scenario: 15,000-paragraph document in DetachedDocumentWindow, append at the end.
  - Result: 6.929 ms in the measured run.

Important: this does not fully close the user's 30-second freeze report.
The measured keystroke path is now light, but the next P0 pass should measure actual app create/load/save/render flow, especially the stress 15k command and any real visible-window layout cost.

Likely next fixes:
- Measure stress 15k create/load/save/render flow in the running app.
- Keep expensive metrics/search/save/preview work out of every keystroke.
- Move expensive work behind timers/background tasks.
- Consider replacing WPF TextBox with AvalonEdit or a custom paragraph/segment editor.
- Add a visible-window typing-latency smoke test around large documents.
- Add explicit segment navigation/edit UX for large documents; current large-document editor opens a bounded front segment.

### P0: Original local workspace is not clean

The public GitHub repo was pushed from:
C:\Users\Masters\AppData\Local\Temp\writer-workbench-public-20260623213732

The active local workspace still has untracked/modified files.
Do not assume C:\Users\Masters\Documents\글 is already committed or synced.

Before future publish/sync work:
- check git status -sb
- decide whether to replace the local remote/history with the clean public repo or keep local history separate
- avoid accidentally publishing old Tauri-era history unless the user explicitly wants it

### P1: Old docs conflict with current direction

Old 2026-06-22 docs mention Tauri/React/WebView assumptions.
Current canonical docs are:
- docs\superpowers\specs\2026-06-23-writer-workbench-wpf-design.md
- docs\superpowers\plans\2026-06-23-writer-workbench-wpf-mvp-core.md

Future agents should read the 2026-06-23 docs first.

### P1: UI Korean must be verified in the running app

MainWindow.xaml has Korean strings, and tests pass.
Still verify in the actual app window after changes because console output can make encoding look broken.

### P1: MainWindow is too behavior-heavy

The app works, but MainWindow.xaml.cs currently owns too much behavior.
After P0 performance is fixed, move toward view-model/service boundaries carefully.

## Planned / next implementation

### Current user instruction / next handoff

The user asked to input the current direction into pingpong, not merely summarize it in chat.

Next agent should treat the following as active work order:

1. Actual running-app e2e performance measurement comes first.
   - The offscreen PerfProbe did not reproduce the 30-second freeze.
   - Do not mark the freeze solved from PerfProbe alone.
   - Measure the real executable path from user action to visible UI response.
   - Target flows:
     - Stress 15k button click to usable editor.
     - Large document load to usable editor.
     - Typing into the loaded stress document.
     - Save/autosave after edit.
     - Preview switch.
     - Detached window open, edit, save.

2. Large document editing UX is still incomplete.
   - Current main editor opens only a bounded front segment of about 4,957 chars.
   - This is a performance guard, not a complete large-manuscript editing design.
   - Add explicit segment navigation/edit workflow before claiming large document editing is usable.

3. Storage format conflict remains unresolved.
   - Current implementation uses `documents/*.wwdoc.json`.
   - Direction note recommends `documents/*.wwtxt` plus `documents/*.meta.json` and SQLite index/cache.
   - Do not harden more project-file behavior until this is decided or migration-safe.

4. Local/public repo alignment is still P0/P1.
   - Public repo is the clean pushed snapshot.
   - Local `C:\Users\Masters\Documents\글` still has untracked project files and old local history.
   - Do not assume local git state is publish-ready.
   - Check public temp repo and local workspace separately before commit/push.

5. Keep scope tight.
   - Do not reopen Tauri/WPF stack debate.
   - Do not add AI/API/Codex/Chrome integration while P0 editor/storage/repo issues are open.
   - Do not report "large text fixed" unless real visible-app measurement supports it.

2026-06-24 KST implementation note:
- User redirected work away from more performance digging:
  - "Do not dig performance; build features."
  - Only check whether the large-manuscript baseline is broken.
  - First feature area: binder scene management.
- Implemented first binder scene management pass:
  - Binder buttons for scene duplicate, delete, move up, move down.
  - ProjectStore scene operations:
    - DuplicateDocumentAsync
    - DeleteDocumentAsync
    - MoveDocumentAsync
  - Manifest order is now preserved instead of always sorting by scene id.
  - SaveDocumentAsync updates an existing manifest entry in place.
  - Delete removes wwdoc/json, derived txt, manifest entry, and SQLite index row.
  - Delete refuses to remove the last remaining scene.
  - AppCommandCatalog was normalized to usable Korean command labels while adding binder commands.
- Verification:
  - dotnet build WriterWorkbench.sln --no-restore
  - dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
  - Result: 83 passed, 0 failed, 0 skipped.
- Large manuscript baseline check only:
  - dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release
  - Latest run: 2026-06-24 01:10:38 KST
  - MainWindow stress command total: 201.346 ms.
  - ProjectStore.SaveDocumentAsync total: 120.252 ms.
  - MainWindow EditorBox segment set: 1.484 ms.
  - Detached append sync: 3.329 ms.
  - No new 30-second path was introduced by binder scene management.

### Immediate next work

1. Continue binder scene management UX from the first pass.
2. Add explicit large-document segment navigation/edit workflow.
3. Verify Korean UI in the actual running app.
4. Clean up local/public repo alignment.
5. Remove or archive superseded Tauri-era docs after user approval.

### MVP 0.1 remaining direction

- Make the editor stable under large manuscript workloads.
- Keep autosave reliable and visible.
- Keep TXT export automatic.
- Keep preview explicit and cheap.
- Keep command/shortcut system as first-class core.
- Improve workspace preset reliability for one monitor first.

### MVP 0.2 direction

- Better detached tab/window workflow.
- Multi-monitor preset restore with user-selected monitor assignment.
- Startup preset auto-apply polish.
- Better search UI.
- Session logs.
- Paragraph tags and review flags.

### MVP 0.3 direction

- Plot/person/reference panels.
- Cover layout tools.
- CSV/JSON data cleanup exports.
- Rule-based manuscript checker.
- Visualization.
- Spellcheck/proofreading strategy.

### Later, opt-in only

- Chrome launcher bridge.
- External API providers.
- Local algorithms.
- AI analysis.
- Codex integration only if it can be done without unsafe account/session scraping.

## Security and privacy notes

- Default app behavior should require no internet.
- Do not send manuscript text externally by default.
- Do not read Chrome cookies/passwords/session tokens.
- Do not store future API keys inside manuscript project folders.
- Do not add hard OS lockout behavior.
- Focus mode must remain app-level and user-controllable.

## Handoff rule

When resuming work, do not start with stack debate.
Use WPF/.NET unless the user explicitly reopens stack choice.

Completion upload rule:
- After every work batch, update pingpong with what changed, what was verified, and what remains.
- Upload the updated pingpong to the public repo before reporting completion.
- Upload related project data to the repo as well when the work produced or changed data files, measurement outputs, fixtures, docs, specs, exports, or other non-sensitive artifacts needed for handoff.
- If no separate data artifact was produced, say that explicitly in the final report.
- Do not leave the public repo behind local-only work unless the user explicitly says not to push.

First read:
1. this file
2. docs\superpowers\specs\2026-06-23-writer-workbench-wpf-design.md
3. docs\superpowers\plans\2026-06-23-writer-workbench-wpf-mvp-core.md

Then check:
1. git status -sb
2. dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore
3. current large-text typing behavior

## 2026-06-24 KST feature implementation update: Binder scene management polish

- Implemented binder scene rename with `document.renameScene`, a binder toolbar `이름` button, and `SceneRenameDialog`.
- New scenes keep automatic sequential ids and immediately focus/select the title box so the title can be edited right away.
- Scene delete now asks for confirmation before deleting and keeps the next available scene selected after deletion.
- Scene duplicate, move up/down, create, delete, and rename status messages include the affected scene id and title.
- Binder order continues to persist through manifest order, and refresh remains manifest/metadata driven instead of loading document bodies.
- `ProjectStore.RenameDocumentAsync` updates the wwdoc metadata, manifest title, TXT export, and SQLite search title together.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/SceneRenameDialog.xaml`
- `src/WriterWorkbench/SceneRenameDialog.xaml.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/SceneRenameDialogTests.cs`

Tests added/expanded:
- create sequential scene ids and title metadata persistence.
- rename manifest/document/search-index synchronization.
- duplicate order persistence after reload.
- delete persistence after reload.
- reorder persistence after reload.
- manifest-only binder metadata load without document body files.
- command catalog and MainWindow smoke coverage for `document.renameScene`.
- scene rename dialog title behavior.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ProjectStoreTests|AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands|SceneRenameDialogTests"` -> 17 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 88 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj --no-restore` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 224.996 ms.
- ProjectStore.SaveDocumentAsync total: 128.336 ms.
- ProjectStore.LoadDocumentAsync: 119.756 ms.
- MainWindow EditorBox segment set: 1.850 ms.
- MainWindow preview switch/render: 1.731 ms.
- Detached append sync: 8.887 ms.

Remaining problems:
- Actual app UI should still be visually checked in a running WPF window after more Korean-label cleanup.
- Large-file typing lag remains a separate editor workflow task; this batch did not investigate performance beyond guardrail checks.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Scene Inspector MVP

- Implemented `SceneMetadata` sidecar storage under `documents/{scene-id}.meta.json`.
- Added `SceneStatus` values for Draft/Revising/Final/Excluded, shown in Korean as 초고/수정중/완료/제외.
- Added `SceneMetadataStore` for default metadata creation, load, save, and delete.
- Metadata JSON stores status as a readable string, for example `"Status": "Revising"`.
- Connected `ProjectStore` so new scenes get default metadata, duplicated scenes copy synopsis/status/tags/target count with a refreshed `UpdatedAt`, and deleted scenes remove metadata files.
- Added a right-panel Scene Inspector MVP below search results with 시놉시스, 상태, 태그, 목표 글자 수, 현재 글자 수, 수정 시각, and a metadata-only 저장 button.
- Active scene load refreshes Inspector metadata. Inspector save writes only `.meta.json`; `EditorBox_TextChanged` still only marks dirty/edit time and does not save metadata or rewrite body files.
- Current character count uses `DocumentMetricsService` during document load/save metric refresh, not on every keystroke.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadata.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadataStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/SceneMetadataStoreTests.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added/expanded:
- `SceneMetadataStoreTests.LoadCreatesDefaultMetadataWhenMissing`
- `SceneMetadataStoreTests.SavesAndLoadsSynopsisStatusTagsAndTargetCharacterCount`
- `SceneMetadataStoreTests.MetadataSaveDoesNotRewriteDocumentBodyFiles`
- `SceneMetadataStoreTests.DeleteRemovesMetadataFileWhenPresent`
- `ProjectStoreTests.CreateDocumentGeneratesSequentialSceneIdsAndStoresEditableTitle` now verifies default metadata.
- `ProjectStoreTests.DuplicateDocumentCopiesSceneAfterSource` now verifies metadata copy behavior.
- `ProjectStoreTests.DeleteDocumentRemovesFilesManifestEntryAndSearchIndex` now verifies metadata cleanup.
- `MainWindowSmokeTests.MainWindowContainsSceneInspectorSurface`

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneMetadataStoreTests|ProjectStoreTests|MainWindowContainsSceneInspectorSurface"` -> 18 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 93 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 233.815 ms.
- ProjectStore.SaveDocumentAsync total: 182.813 ms.
- ProjectStore.LoadDocumentAsync: 169.107 ms.
- MainWindow EditorBox segment set: 2.008 ms.
- MainWindow preview switch/render: 2.410 ms.
- Detached append sync: 5.305 ms.
- Detached SaveAsync after edit: 302.007 ms.
- Save path stayed under the user baseline of 500 ms in this probe.

Remaining problems:
- Inspector UI is MVP only; no tag autocomplete, search/inspector tab switch, or command-palette metadata command yet.
- Actual running WPF UI still needs visual inspection for right-panel spacing and Korean label fit.
- Excluded scenes are stored for future Export MVP, but export skipping is not implemented yet.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Export MVP

- Implemented explicit TXT export commands for the current scene and full manuscript.
- Added `exports/` as the export destination under each `.writerproj` project root.
- Current scene export writes only the active scene body to UTF-8 TXT.
- Full manuscript export follows binder/manifest order and joins included scenes into one UTF-8 TXT file.
- Full manuscript export skips scenes whose `SceneMetadata.Status` is `Excluded`.
- Export output path is shown in the status message after success.
- Export is connected only through explicit buttons/commands: `export.currentScene` and `export.fullManuscript`.
- `EditorBox_TextChanged` remains dirty/edit-time only; export is not connected to the input loop.
- Added read-only metadata loading through `SceneMetadataStore.LoadExistingOrDefaultAsync` so full export can inspect status without creating or rewriting metadata files.

Files changed:
- `src/WriterWorkbench/Core/Export/ManuscriptExportService.cs`
- `src/WriterWorkbench/Core/Export/ManuscriptExportResult.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadataStore.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/ManuscriptExportServiceTests.cs`
- `tests/WriterWorkbench.Tests/SceneMetadataStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added/expanded:
- current scene export writes UTF-8 TXT under `exports/`.
- full manuscript export preserves binder order.
- full manuscript export skips `SceneStatus.Excluded`.
- Korean body text is preserved through UTF-8 export.
- export creates the `exports/` directory when missing.
- no-included-scene and empty-manifest full export fail without writing a misleading output file.
- export does not mutate document JSON, sidecar TXT, or metadata JSON.
- full export does not create a missing metadata sidecar just to inspect status.
- repeated explicit exports create distinct TXT files instead of overwriting the prior export.
- command catalog and MainWindow smoke coverage for export commands/buttons.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExportFullManuscriptDoesNotCreateMissingMetadataFile"` -> 1 failed before the metadata read-only fix, then 1 passed after the fix.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExportCurrentSceneDoesNotOverwriteRepeatedExports"` -> 1 failed before unique export suffixes, then 1 passed after the fix.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ManuscriptExportServiceTests|SceneMetadataStoreTests|AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands"` -> 16 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ManuscriptExportServiceTests"` -> 8 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 102 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj --no-restore` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 386.512 ms.
- ProjectStore.SaveDocumentAsync total: 207.487 ms.
- ProjectStore.LoadDocumentAsync: 110.425 ms.
- MainWindow EditorBox segment set: 1.570 ms.
- MainWindow preview switch/render: 1.904 ms.
- Detached append sync: 7.561 ms.
- Export MVP was checked as an explicit command path only; no export work was added to typing/input events.

Remaining problems:
- Export MVP is TXT-only; no compile format profiles, headings, chapter separators, cover-layout export, or per-status export filters beyond `Excluded`.
- Actual running WPF UI still needs visual inspection for button placement and Korean label fit.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Export MVP result polish

- Updated `ManuscriptExportResult` to report included scene count, excluded scene count, and exported character count.
- Full manuscript export now writes each included scene with a `# scene title` heading before the body.
- Full manuscript export still follows binder/manifest order and skips `SceneStatus.Excluded`.
- Current scene export still writes only the active scene body, but now returns included/excluded/character-count metadata.
- UI status messages now show included count, excluded count for full export, character count, and output path.
- Repeated explicit exports still create distinct TXT files through the timestamp plus unique suffix filename.
- Export remains command/button only; no export work was added to `EditorBox_TextChanged` or other input-loop handlers.

Files changed:
- `src/WriterWorkbench/Core/Export/ManuscriptExportResult.cs`
- `src/WriterWorkbench/Core/Export/ManuscriptExportService.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/ManuscriptExportServiceTests.cs`

Tests added/expanded:
- current scene export asserts included count, excluded count, and character count.
- full manuscript export asserts included count, excluded count, character count, Korean scene headings, Korean body text, binder order, and excluded-scene skipping.
- missing metadata full export now accepts the heading-bearing full manuscript format while still proving no metadata sidecar is created.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ManuscriptExportServiceTests"` -> failed before result metadata fields existed, then 8 passed after the fix.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 102 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 419.704 ms.
- ProjectStore.SaveDocumentAsync total: 261.214 ms.
- ProjectStore.LoadDocumentAsync: 181.303 ms.
- MainWindow EditorBox segment set: 1.418 ms.
- MainWindow preview switch/render: 1.846 ms.
- Detached append sync: 13.541 ms.
- TXT export sub-cost in the perf probe: 59.444 ms.

Remaining problems:
- Export is still TXT-only; DOCX/PDF/EPUB, export templates, per-tag filters, and title-output options are intentionally out of scope.
- Full export heading format is fixed as `# title`; this should become an export option later.
- Actual running WPF UI still needs visual inspection for status-message fit.
- No separate data artifact was produced; the perf result was not saved as a file.

## 2026-06-24 KST feature implementation update: Snapshot / Backup MVP

- Implemented manual current-scene snapshot creation.
- Snapshots are stored under `snapshots/{scene-id}/{snapshot-id}/`.
- Each snapshot stores fixed files: `snapshot.info.json`, `document.wwdoc.json`, `document.txt`, and `scene.meta.json` when scene metadata exists.
- Added active-scene snapshot listing in the right Scene Inspector panel.
- Added selected snapshot restore and selected snapshot delete commands.
- Restore rewrites the document through `ProjectStore.SaveDocumentAsync`, so document JSON, derived TXT, manifest title, and SQLite search index are refreshed together.
- Restore also restores the scene metadata sidecar from the snapshot.
- Restore and snapshot delete both show confirmation dialogs before changing files.
- Scene delete now offers an optional pre-delete snapshot after delete confirmation and before file removal.
- Snapshot restore offers an optional pre-restore snapshot before overwriting the current scene.
- Large manual overwrite saves offer an optional pre-save snapshot; autosave and snapshot-internal saves do not show this prompt, so the editor input loop is not blocked.
- `EditorBox_TextChanged` remains dirty/edit-time only; snapshot work is not connected to typing.

Files changed:
- `src/WriterWorkbench/Core/Snapshots/SceneSnapshotInfo.cs`
- `src/WriterWorkbench/Core/Snapshots/SceneSnapshotService.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/SceneSnapshotServiceTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added/expanded:
- create snapshot writes document JSON, TXT, and metadata under the scene snapshot folder.
- list snapshots returns only the active scene snapshots, newest first.
- restore snapshot restores title, body, metadata, and SQLite search index.
- delete snapshot removes only the snapshot folder and keeps live document files.
- missing snapshot restore throws without mutating the document.
- command catalog coverage for `snapshot.createCurrent`, `snapshot.restoreSelected`, and `snapshot.deleteSelected`.
- MainWindow smoke coverage for snapshot buttons and active-scene snapshot list.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneSnapshotServiceTests|AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands|MainWindowContainsSceneInspectorSurface"` -> failed before `Core.Snapshots` existed, then 9 passed after implementation.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 107 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release` -> completed.

Large manuscript guardrail:
- MainWindow stress command total: 265.677 ms.
- ProjectStore.SaveDocumentAsync total: 154.065 ms.
- ProjectStore.LoadDocumentAsync: 81.781 ms.
- MainWindow EditorBox segment set: 1.513 ms.
- MainWindow preview switch/render: 1.526 ms.
- Detached append sync: 7.204 ms.
- Snapshot MVP adds no work to typing/input events.

Remaining problems:
- Snapshot UI is MVP-only; no snapshot notes, diff view, preview, pinning, or bulk cleanup yet.
- Optional snapshot prompts are UI-level behavior and are covered by command/smoke tests plus service tests, not automated modal-click tests.
- Large overwrite snapshot prompt is skipped for autosave by design to avoid blocking the writing loop.
- No separate data artifact was produced; the tests create only temporary project data and no reusable fixture/sample project was added to the repo.

## 2026-06-24 KST research planning update: LLL reverse-engineering safety plan

Scope:
- Added a written LLL structure reference and reverse-engineering plan for Writer Workbench.
- This is a planning document only; no new LLL filesystem scan was performed for this update.
- The plan uses previously observed high-level structure and explicitly avoids executable reverse engineering, WebView/cache/DB folders, browser storage, lock files, and manuscript text extraction.

Files changed:
- `docs/research/lll-reverse-engineering-plan.md`
- `docs/pingpong.txt`

Safety guardrail:
- LLL `EBWebView`, `Cache`, `Code Cache`, `GPUCache`, `Crashpad`, `Network`, `IndexedDB`, `Local Storage`, `Session Storage`, and `Service Worker` remain excluded.
- Any future LLL inspection must be shallow, bounded, and schema-oriented.
- Large recursive scans remain forbidden.

Commands run:
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 107 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 107 passed.

Data artifact:
- 별도 데이터 산출물 없음.

Remaining problems:
- The actual LLL structure report has not been written yet.
- The Writer Workbench implementation plan derived from LLL has not been written yet.
- Future investigation must stop at schema/metadata and must not read user manuscript body text.

## 2026-06-24 KST research update: LLL schema observation and Writer Workbench plan

Scope:
- Completed a bounded LLL schema observation after the safety plan.
- Observed only top-level app data, `kv` JSON setting schemas, app index JSON schemas, window-state schema, one project manifest schema, and one episode schema.
- Did not inspect executables, network traffic, WebView internals, cache folders, browser DB/storage folders, or lock files.
- Did not output manuscript body text.

Files changed:
- `docs/research/lll-structure-report.md`
- `docs/research/lll-to-writer-workbench-plan.md`
- `docs/research/lll-reverse-engineering-plan.md`
- `docs/pingpong.txt`

Key findings:
- LLL separates app data, global app indexes, roaming window state, and user project folders.
- Project folders use a project manifest file plus `episodes`, `resources`, and `wiki` folders.
- Episode schema includes id, novelId, title, description, content length fields, episode type, status, order, manualLineBreak, timestamps, blocks, and comments.
- App settings separate editor style/options, wiki editor style/options, workspace options, export settings, view options, widget settings, and legacy editor style.
- Text replacement rules use `from`, `to`, `useRegexp`, and `enabled`.
- Widget registry instances use `id`, `widgetId`, `context`, `surface`, `area`, `order`, and `frame`.
- Window state is stored as rectangle/state flags for the main window.

Writer Workbench plan:
- Keep current body/metadata/index separation.
- Strengthen `SceneMetadata` with content length, type, manual line break, timestamps, tags, and summary.
- Split app settings by function.
- Treat graphics presets as editor/theme profiles, not only color palettes.
- Add Widget Registry MVP around command ids.
- Add Text Replacement MVP as manual command first.
- Add Export Profile MVP after TXT export.
- Strengthen Window Placement Preset with screen-slot fallback.

Commands run:
- Bounded Node schema extraction over known LLL paths only.
- No large recursive scan.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 107 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 107 passed.

Data artifact:
- No separate fixture/sample project was added. The durable research artifacts are the markdown reports above.

Remaining problems:
- No Writer Workbench code was changed in this update.
- Follow-up implementation should start with `SceneMetadata` strengthening or settings-file separation.
- Any future LLL observation must keep the same exclusion list and schema-only output rule.

## 2026-06-24 KST docs cleanup: LLL anonymization

Scope:
- Replaced the external reference app name with `LLL` in research docs and pingpong.
- Renamed research document filenames from the original app-name prefix to `lll-*`.
- Anonymized app-specific project/episode extension examples in research docs.
- Did not access the external app folders for this cleanup.

Files changed:
- `docs/research/lll-reverse-engineering-plan.md`
- `docs/research/lll-structure-report.md`
- `docs/research/lll-to-writer-workbench-plan.md`
- `docs/pingpong.txt`

Verification:
- Search for the previous app name in repo docs/files returned no matches.
- Search for app-specific project/episode extension examples in repo docs/files returned no matches.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 107 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 107 passed.

## 2026-06-24 KST feature implementation update: SceneMetadata strengthening from LLL research

Feature(s) implemented:
- Strengthened `SceneMetadata` to schema version 2 with derived counts, scene type, manual line break flag, created timestamp, and summary.
- Kept existing `SceneStatus` behavior and normalized older schema-1 metadata files with defaults.
- Preserved the legacy `Synopsis` field as a compatibility mirror of `Summary`.
- Added derived metadata refresh during `ProjectStore.SaveDocumentAsync`, so body saves update content counts and `UpdatedAt`.
- Kept metadata saving separate from body saving; `SceneMetadataStore.SaveAsync` writes only `scene.meta.json`.
- Added inspector fields for content length without spaces, content length with spaces, scene type, and manual line break.
- Updated binder/recent list item construction to use manifest plus scene metadata instead of reading document bodies.
- `EditorBox_TextChanged` was not connected to count calculation, export, snapshot, or index rebuild work.

Files changed:
- `src/WriterWorkbench/Core/Storage/SceneMetadata.cs`
- `src/WriterWorkbench/Core/Storage/SceneMetadataStore.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/SceneMetadataStoreTests.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added:
- Existing metadata without new fields loads with schema-2 defaults.
- Default metadata includes new field defaults.
- Content length and content length with spaces save/load.
- Tags, summary, scene type, and manual line break roundtrip.
- Metadata save does not rewrite document JSON/TXT body files.
- Body save refreshes derived counts.
- Korean body count preserves Hangul character counts with and without spaces.
- Scene Inspector smoke test covers the new metadata display controls.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneMetadataStoreTests|SaveDocumentUpdatesDerivedSceneMetadataCountsForKoreanBody|SaveDocumentPreservesManualSceneMetadataAndRefreshesDerivedCounts"` -> failed before production fields existed, then 8 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowContainsSceneInspectorSurface"` -> failed before new inspector controls existed, then included in the passing focused run.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneMetadataStoreTests|SaveDocumentUpdatesDerivedSceneMetadataCountsForKoreanBody|SaveDocumentPreservesManualSceneMetadataAndRefreshesDerivedCounts|MainWindowContainsSceneInspectorSurface"` -> 9 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 110 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 110 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release` in the public repo -> completed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release --no-build` in the public repo -> completed.

Large manuscript regression check, if touched:
- First perf run: ProjectStore.SaveDocumentAsync total 366.079 ms; MainWindow stress command total 1,074.765 ms; MainWindow EditorBox segment set 1.276 ms; MainWindow preview switch/render 2.373 ms; Detached append sync 6.508 ms.
- Second perf run: ProjectStore.SaveDocumentAsync total 264.965 ms; MainWindow stress command total 799.149 ms; MainWindow EditorBox segment set 0.527 ms; MainWindow preview switch/render 54.879 ms; Detached append sync 9.482 ms.
- Typing path remains unchanged: `EditorBox_TextChanged` only marks dirty and edit time.

Remaining problems:
- There is no separate outline surface yet; binder/recent list and inspector are now metadata-backed, but a full outline panel remains future work.
- Binder display is metadata-rich but visually dense; later UI polish should decide which fields are always visible versus tooltip/detail-only.
- Derived counts are refreshed on body save, not on every keystroke by design.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-27 KST regression repair update: HTML binder and remote control loop review

Feature(s) implemented:
- Applied the requested two-pass loop: structure report -> external-style review -> improvements, repeated twice.
- Added the loop report at `docs/reports/html-workbench-regression-loop-2026-06-27.md`.
- Restored HTML binder functionality with action bar, context menu, scene selection, and scene-scoped command bridge.
- Added WPF bridge handling for `document.select` and `document.command`.
- Binder commands now select the target binder item before running; binder-only commands avoid unnecessary document/WebView loading.
- Restored native topmost remote as the primary remote while HTML workbench is visible.
- HTML workbench entry no longer hides the native remote layer.
- Added cross-arrow move glyph to the native remote handle.
- Hid the in-WebView floating remote by default to avoid competing with the native remote.

Files changed:
- `docs/reports/html-workbench-regression-loop-2026-06-27.md`
- `docs/pingpong.txt`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/RemoteControlLayerWindowTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`

Tests added/updated:
- HTML asset tests now lock binder action/context anchors, document command messages, and remote move icon.
- MainWindow tests now lock native remote availability in HTML workbench and binder command routing.
- RemoteControlLayerWindow test now locks the cross-arrow move glyph.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate"` -> failed before loop 1 implementation, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowHtmlBinderCommandSelectsSceneBeforeRunningBinderCommand` -> initially timed out due test deadlock, fixed test setup, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate|MainWindowHtmlWorkbenchKeepsNativeRemoteControlLayerAvailable|MainWindowHtmlBinderCommandSelectsSceneBeforeRunningBinderCommand"` -> 4 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "LayerIsTopmostDraggableAndRendersRemotePlacements|MainWindowShowingHtmlWorkbenchDoesNotHideVisibleNativeRemoteControlLayer|WebWorkbenchCssKeepsInWindowRemoteSecondary"` -> failed before loop 2 implementation, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate|MainWindowHtmlWorkbenchKeepsNativeRemoteControlLayerAvailable|MainWindowHtmlBinderCommandSelectsSceneBeforeRunningBinderCommand|LayerIsTopmostDraggableAndRendersRemotePlacements|MainWindowShowingHtmlWorkbenchDoesNotHideVisibleNativeRemoteControlLayer|WebWorkbenchCssKeepsInWindowRemoteSecondary"` -> 7 passed.
- `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8')); console.log('app.js syntax ok')"` -> passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 191 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-menu-remote` -> passed.
- `node -e "new Function(require('fs').readFileSync('dist/WriterWorkbench-menu-remote/WebWorkbench/app.js','utf8')); console.log('dist app.js syntax ok')"` -> passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- Binder actions operate on manifest/selection and do not read all document bodies.

Remaining problems:
- Settings/reference rails are still incomplete work surfaces.
- Relationship map HTML view is still a shell while the functional relationship map remains native.
- HTML shortcut screen remains read-only; shortcut edit/save bridge is not implemented yet.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. The loop report is documentation, not sample project data.

## 2026-06-27 KST feature implementation update: HTML workbench direct editing and active side panels

Feature(s) implemented:
- HTML workbench center surface now exposes the active scene title and editable body directly.
- HTML active scene payload includes only the active document's bounded editor text, not every binder document body.
- HTML body/title input posts `activeScene.update` messages back to WPF with a short debounce.
- Command clicks flush pending HTML editor updates before executing save/export/snapshot/etc.
- WPF WebView message handling now accepts `activeScene.update` and applies it to the current `WriterDocument`, hidden native editor box, metrics, and dirty/autosave state.
- HTML left rail tabs now switch between `바인더`, `설정집`, and `자료` panels.
- `설정집` panel renders story/tool command buttons from the menu command payload.
- `자료` panel renders current project/scene/summary reference information.
- HTML remote now has a `이동` handle and can be dragged by pointer.

Files changed:
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- HTML payload includes active editor text and segment metadata.
- HTML shell contains active body editor, settings/reference panels, and remote drag handle.
- HTML script wires remote dragging, rail tab switching, and active scene update messages.
- MainWindow applies HTML active-scene editor updates to the current document, native editor fields, and dirty state.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CreatesDashboardPayloadWithActiveEditorTextOnly|WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate|MainWindowAppliesHtmlActiveSceneEditorUpdateToCurrentDocument"` -> failed before implementation with missing payload fields, then 4 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 181 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release --self-contained false -o dist\WriterWorkbench-menu-remote` -> succeeded.
- Public repo: `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo: `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 181 passed.

Large manuscript regression check, if touched:
- HTML payload uses `DocumentEditorTextService.CreateView`, so only the active bounded editor segment is sent to the HTML workbench.
- Binder/metadata payload still does not read every manuscript body.
- No export/save work was added to `EditorBox_TextChanged` or the HTML input loop; HTML input updates in-memory current document/dirty state and existing autosave/manual save does the disk write.

Remaining problems:
- HTML workbench editor is still plain-text textarea, not rich paragraph/block editing.
- `설정집` and `자료` panels are active MVP panels; full settings-book/reference library CRUD is still pending.
- Remote drag position is session-only inside the HTML workbench and is not yet persisted.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-26 KST feature implementation update: GitHub-style command contribution foundation

Feature(s) implemented:
- Applied a VS Code-style command contribution structure from trusted GitHub references.
- Added `CommandContribution` and `CommandPlacementContribution`.
- `CommandRegistry` now stores commands, default UI placements, and default shortcuts together.
- Default toolbar, remote-control, and shortcut bootstrap now derive from command contribution data instead of scattered hardcoded lists.
- `WorkbenchCustomizationProfileFactory` now creates editable profile placements/shortcuts from the registry contribution data.
- `ShortcutProfileService` now builds default shortcuts from the default command registry.
- Added a research note explaining the GitHub reference structure and what was adapted.

Files changed:
- `src/WriterWorkbench/Core/Commands/CommandContribution.cs`
- `src/WriterWorkbench/Core/Commands/CommandRegistry.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Commands/ShortcutManager.cs`
- `src/WriterWorkbench/Core/Commands/ShortcutProfileService.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `tests/WriterWorkbench.Tests/CommandContributionTests.cs`
- `docs/research/github-workbench-architecture-references.md`
- `docs/pingpong.txt`

Tests added:
- Registry stores command placements and shortcuts from contributions.
- Default catalog exposes toolbar, remote-control, and shortcut contributions.
- Registry rejects duplicate contribution slots and shortcut conflicts.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter CommandContributionTests` -> failed before implementation because contribution types/APIs did not exist.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CommandContributionTests|WorkbenchCustomizationProfileTests|ShortcutManagerTests"` -> 16 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 160 passed.
- `dotnet publish .\src\WriterWorkbench\WriterWorkbench.csproj -c Release -o .\dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 160 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- This change only reorganizes command metadata and profile bootstrap.

Remaining problems:
- MainWindow still owns many command handlers directly; the next structural step is a command execution pipeline/hook layer.
- HTML/WebView2 workbench surface is not implemented yet.
- Context menus still need to be fully generated from contribution/profile data.

Separate data artifact 여부:
- `docs/research/github-workbench-architecture-references.md` added as a design/reference artifact.

## 2026-06-26 KST feature implementation update: HTML Workbench MVP start

Feature(s) implemented:
- Started the HTML conversion path without replacing the Windows-native engine.
- Added WebView2 dependency for an in-app local HTML workbench surface.
- Added `view.htmlWorkbench.open` / `HTML 작업대` command.
- Added `AppSessionState.HtmlWorkbenchSurface` so the HTML workbench can be restored as the last active surface.
- Added a WebView2 host surface inside MainWindow while keeping WPF editor/preview/main/relationship-map fallback surfaces.
- Added local HTML/CSS/JS assets under `src/WriterWorkbench/WebWorkbench`.
- Added a WebView2 bridge: WPF posts project/binder/active-scene/command state to HTML, and HTML posts command IDs back to existing `ExecuteCommandAsync`.
- Added `WebWorkbenchPayloadFactory` so the HTML UI receives metadata and command placement data without large manuscript body text.
- Published local executable with bundled `WebWorkbench/index.html`, `styles.css`, and `app.js`.

Files changed:
- `src/WriterWorkbench/WriterWorkbench.csproj`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/Core/Application/AppSessionState.cs`
- `src/WriterWorkbench/Core/Application/AppSessionStateService.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Workspace/WorkbenchSurfaceCatalog.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `docs/superpowers/plans/2026-06-26-html-workbench-mvp.md`
- `docs/pingpong.txt`

Tests added:
- Web workbench payload excludes manuscript body text.
- Web workbench payload preserves Korean project/status text.
- Web workbench assets are copied next to the application output.
- MainWindow contains the HTML workbench surface and can switch to it.
- App command catalog exposes the new `HTML 작업대` command.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchPayloadFactoryTests` -> failed before WebWorkbench namespace existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> failed before assets/copy settings existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "AppCommandCatalogTests|MainWindowContainsWorkspacePresetCommands|MainWindowContainsLongOperationProgressSurface"` -> failed before `view.htmlWorkbench.open` existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowCanShowHtmlWorkbenchSurface` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchPayloadFactoryTests|WebWorkbenchAssetTests|AppCommandCatalogTests|MainWindowCanShowHtmlWorkbenchSurface|MainWindowContainsWorkspacePresetCommands|MainWindowContainsLongOperationProgressSurface"` -> 7 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 163 passed.
- `dotnet publish .\src\WriterWorkbench\WriterWorkbench.csproj -c Release -o .\dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 163 passed.
- Public repo `dotnet restore WriterWorkbench.sln` -> restored after adding WebView2 package.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 163 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors after rerunning build alone. The first parallel build/test attempt raced WPF generated files and build failed while the parallel test passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/render/save paths were not touched.
- HTML workbench payload intentionally excludes full manuscript body text.
- HTML bridge only sends metadata, binder summaries, command placements, status text, and active scene metadata.

Remaining problems:
- The HTML surface is an MVP dashboard; the main editor is still WPF.
- WebView2 initialization was not run inside automated tests because STA test initialization can hang without a normal app message loop.
- Relationship map, settings book, and timeline still need HTML-native surfaces.
- Context menus and remote-control editing still render in WPF.

Separate data artifact 여부:
- `docs/superpowers/plans/2026-06-26-html-workbench-mvp.md` added.
- Local HTML assets added under `src/WriterWorkbench/WebWorkbench`.
- NuGet package added: `Microsoft.Web.WebView2` 1.0.4022.49, package cache size observed at about 9.6 MB.

## 2026-06-26 KST correction update: HTML workbench should not look nested

Problem corrected:
- The first HTML Workbench MVP placed `HtmlWorkbenchSurface` inside the center editor column.
- That made the HTML UI look like a separate mini-program embedded inside the WPF program.
- The visual style also leaned too much toward old desktop/dashboard controls.

Feature(s) implemented:
- Moved `HtmlWorkbenchSurface` to a full work-area overlay spanning binder/editor/inspector columns.
- HTML workbench mode now hides the WPF native command chrome, WPF status line, and metrics line.
- Returning to editor/main/preview/relationship-map restores the native WPF chrome.
- Replaced the first CSS with a flatter, cleaner, more modern writing-workbench style.
- Removed the extra WPF `HTML 작업대` header that made the WebView feel nested.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- HTML workbench surface now verifies `Grid.ColumnSpan == 3`.
- HTML workbench mode verifies native WPF chrome/status/metrics are hidden.
- Returning to the main surface verifies native WPF chrome/status/metrics are restored.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowCanShowHtmlWorkbenchSurface|MainWindowContainsLongOperationProgressSurface|WebWorkbenchAssetTests"` -> 3 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 163 passed.
- `dotnet publish .\src\WriterWorkbench\WriterWorkbench.csproj -c Release -o .\dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.

Large manuscript regression check, if touched:
- Large manuscript editor input/render/save paths were not touched.
- This change only adjusts the HTML workbench host placement and CSS.

Remaining problems:
- HTML workbench still renders a dashboard/home surface, not the full editor yet.
- The WPF menu is hidden only in HTML workbench mode; deeper conversion still needs a true HTML editor surface or command shell.
- Visual quality still needs direct user review from the published executable.

Separate data artifact 여부:
- Updated bundled `src/WriterWorkbench/WebWorkbench/styles.css`.

## 2026-06-24 KST feature implementation update: Story Structure Foundation + Relationship Map MVP 1

Feature(s) implemented:
- Added a project-level `story/story.structure.json` store for story structure nodes and relationship links.
- Added `StoryStructureDocument`, `StoryStructureNode`, `RelationshipLink`, and `StoryStructureStore`.
- Added story path helpers to `ProjectPaths`.
- Added command ids and catalog entries for `story.structure.addNode` and `story.relationship.add`.
- Added a Korean main-workbench panel for story structure / relationship entry and lists.
- Story node creation is explicit button/command only and can link the current active scene id without reading or rewriting the scene body.
- Relationship creation validates that both endpoints exist in the story structure before saving.
- Story structure save remains separate from scene body save, metadata save, export, snapshot, and typing loops.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureDocument.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureStore.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/StoryStructureStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added:
- Empty story structure file creation.
- Story node and relationship roundtrip.
- Broken relationship endpoint cleanup.
- Story structure save does not rewrite document JSON/TXT bodies.
- Korean UTF-8 preservation in story structure JSON.
- Command catalog coverage for story structure commands.
- MainWindow smoke coverage for the story structure / relationship panel.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "StoryStructureStoreTests|AppCommandCatalogTests|MainWindowContainsStoryStructureSurface|MainWindowContainsWorkspacePresetCommands"` in the public repo -> failed before production `Core.Story` types existed.
- Same focused test command in the public repo after implementation -> 9 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 116 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release --no-restore` in the public repo -> completed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 116 passed.

Large manuscript regression check, if touched:
- MainWindow was touched for a new panel and command handlers, but `EditorBox_TextChanged`, export, snapshot, preview render, and save loops were not connected to story structure writes.
- Perf guardrail completed: MainWindow stress command total 201.966 ms; MainWindow EditorBox segment set 0.478 ms; MainWindow segment layout/render 18.041 ms; MainWindow preview switch/render 1.310 ms; ProjectStore.SaveDocumentAsync total 155.254 ms; Detached append sync 4.807 ms.

Remaining problems:
- Relationship Map MVP 1 is currently a structured list foundation, not a visual graph canvas.
- Story nodes and relationships can be added, but edit/delete/reorder/preset assignment is still future work.
- Story structure data is not yet surfaced in binder, outline, review panel, export, or snapshot.
- No LLL file format compatibility, UI clone, original app data extraction, WebView/cache access, or AI/API integration was added.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary project data.

## 2026-06-25 KST feature implementation update: Scene Entity Link MVP

Feature(s) implemented:
- Added `story/scene-entity-links.json` as a separate story-side link file.
- Added `SceneEntityLink` and `SceneEntityLinkStore`.
- Scene/entity links store scene id, entity id, role, notes, created timestamp, and updated timestamp.
- Missing scene/entity link file loads as an empty list without creating a file.
- Link add/update validates that the target story entity exists.
- Link add/update upserts by scene id + entity id, preserving created timestamp.
- Entity deletion now removes related scene/entity links.
- Added explicit commands:
  - `sceneEntity.link.add`
  - `sceneEntity.link.delete`
- Added a Scene Inspector "장면 연결" section with entity selector, role field, notes field, link list, connect button, and unlink button.
- Link refresh uses story metadata/entity files only; it does not open or rewrite scene body files.
- No AI extraction, manuscript parsing, automatic tagging, LLL compatibility, or export/snapshot integration was added.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureDocument.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureStore.cs`
- `src/WriterWorkbench/Core/Story/SceneEntityLinkStore.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/SceneEntityLinkStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Missing scene/entity link file returns an empty list.
- Add/update rejects missing entity id.
- Korean link role/notes roundtrip through UTF-8 JSON.
- Add/update upserts the same scene/entity pair.
- Deleting a story entity removes related scene/entity links.
- Saving scene/entity links does not rewrite document JSON/TXT bodies.
- Command catalog covers scene/entity link add/delete commands.
- MainWindow smoke covers Scene Inspector link controls and command buttons.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "SceneEntityLinkStoreTests|AppCommandCatalogTests|MainWindowContainsSceneInspectorSurface|MainWindowContainsWorkspacePresetCommands"` in the public repo -> failed before `SceneEntityLinkStore`, `SceneEntityLinksPath`, and command IDs existed.
- Same focused test command after implementation -> 10 passed.
- Same focused test command after inspector scroll polish -> 10 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 123 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release --no-restore` in the public repo -> completed.
- `dotnet build WriterWorkbench.sln --no-restore` in the local workspace -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 123 passed.

Large manuscript regression check, if touched:
- MainWindow was touched for a Scene Inspector subpanel and command handlers, but `EditorBox_TextChanged`, document input, export, snapshot, preview render, and manuscript parsing paths were not connected to scene/entity link writes.
- Perf guardrail completed: MainWindow stress command total 305.393 ms; MainWindow EditorBox segment set 0.574 ms; MainWindow segment layout/render 34.924 ms; MainWindow preview switch/render 1.811 ms; ProjectStore.SaveDocumentAsync total 179.783 ms; Detached append sync 4.841 ms.

Remaining problems:
- Scene/entity links are not yet shown in binder, outline, export, snapshot detail, timeline, or settings book.
- Link role is a free text field for MVP; preset role options and validation can be polished later.
- Unlink currently removes the selected link directly; richer confirmation/undo can be added when link editing becomes heavier.
- Relationship map lines still rerender after drag-end only; live line-following remains future polish.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests and perf probe create only temporary project data.

## 2026-06-25 KST verification update: Relationship Map manual workflow coverage

Feature(s) implemented:
- No production feature change in this pass.
- Added an actual Relationship Map workflow smoke test after user reported that the map did not behave as a working feature.
- The new test opens the relationship map command, creates two entities, creates a relationship, and verifies that:
  - the relationship map surface is visible
  - two entity list items exist
  - both entity nodes render on the Canvas
  - a relationship line renders on the Canvas
  - the relationship label renders on the Canvas

Files changed:
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- `MainWindowSmokeTests.RelationshipMapCommandAddsEntitiesRelationshipAndRendersMap`

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RelationshipMapCommandAddsEntityAndRendersNode"` in the local workspace -> first failed due missing WPF dispatcher context in the test harness.
- Same test after adding dispatcher pumping and moving sync context to the correct test -> passed as the renamed full relationship map workflow test.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RelationshipMapCommandAddsEntitiesRelationshipAndRendersMap"` in the local workspace -> 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the local workspace -> 124 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RelationshipMapCommandAddsEntitiesRelationshipAndRendersMap"` in the public repo -> 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 124 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench --no-restore` in the local workspace -> completed.

Large manuscript regression check, if touched:
- Production code was not touched in this pass.
- No editor, save, export, snapshot, preview, or large manuscript input path changed.

Remaining problems:
- User-visible relationship map usability is still thin even though the manual code path now has end-to-end coverage.
- The current map remains a minimal manual canvas, not a polished graph editor.
- User-reported symptom may be a local executable/version/UX issue if it still does not work from `dist\WriterWorkbench\WriterWorkbench.exe`; this pass added coverage but did not change production behavior.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary project data.

## 2026-06-24 KST feature implementation update: Relationship Map MVP split story store + canvas

Feature(s) implemented:
- Replaced the first-pass single `story/story.structure.json` direction with split story files:
  - `story/entities.json`
  - `story/relationships.json`
  - `story/relation-layout.json`
- Added `StoryEntity`, `StoryEntityType`, `StoryRelationship`, and `StoryMapNodeLayout`.
- Expanded `StoryStructureStore` with entity/relationship/layout load-save APIs.
- Missing story files now load as empty lists without creating files.
- Added explicit entity add/update/delete and relationship add/update/delete store operations.
- Deleting an entity removes related relationships and saved map layout entries.
- Relationship creation/update rejects missing source/target entity ids.
- Added `story.relationshipMap.open` command plus relationship map command handlers.
- Added a Relationship Map surface with:
  - character/entity list
  - relationship list
  - Canvas node display
  - relationship lines
  - relationship labels
  - character edit form
  - relationship edit form
  - node drag movement
  - drag-end layout persistence
- Kept story structure operations explicit-command only; no AI extraction, no manuscript parsing, no automatic graph layout, no 3D graph, no LLL compatibility.

Files changed:
- `src/WriterWorkbench/Core/Application/AppSessionState.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureDocument.cs`
- `src/WriterWorkbench/Core/Story/StoryStructureStore.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/StoryStructureStoreTests.cs`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Missing split story files return empty lists.
- Entity, relationship, and relation layout files roundtrip Korean UTF-8 text.
- Missing relationship endpoint is rejected.
- Entity delete removes related relationships and layout entries.
- Node layout save updates one entity position.
- Story structure save does not rewrite document JSON/TXT bodies.
- Command catalog covers relationship map and entity/relationship edit commands.
- MainWindow smoke covers relationship map surface, lists, Canvas, and edit controls.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "StoryStructureStoreTests|AppCommandCatalogTests|MainWindowContainsStoryStructureSurface|MainWindowContainsWorkspacePresetCommands"` in the public repo -> failed before new split APIs and commands existed.
- Same focused test command after implementation -> 10 passed.
- `dotnet build WriterWorkbench.sln --no-restore` in the public repo -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` in the public repo -> 117 passed.
- `dotnet run --project tools\WriterWorkbench.PerfProbe\WriterWorkbench.PerfProbe.csproj -c Release --no-restore` in the public repo -> completed.

Large manuscript regression check, if touched:
- MainWindow was touched for the relationship map surface, but `EditorBox_TextChanged`, document save, export, snapshot, preview render, and manuscript parsing paths were not connected to relationship map writes.
- Perf guardrail completed: MainWindow stress command total 498.060 ms; MainWindow EditorBox segment set 0.356 ms; MainWindow segment layout/render 38.567 ms; MainWindow preview switch/render 3.017 ms; ProjectStore.SaveDocumentAsync total 297.639 ms; Detached append sync 5.795 ms.

Remaining problems:
- The map is a first-pass manual Canvas, not an advanced graph editor.
- Lines rerender after drag-end save; live line-following during drag is future polish.
- Entity/relationship edit is functional but still compact and utilitarian; later work should improve selection states and validation messages.
- Story entities are not yet linked to scenes, settings book, timeline, export, snapshots, or binder metadata.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary project data.

## 2026-06-25 KST audit update: NULL evidence and relationship-map reference gap

Audit report:
- `docs/null-evidence-and-reference-audit-2026-06-25.md`

What was checked:
- Public repo head: `332dbbd Add relationship map workflow coverage`.
- Local workspace: `C:\Users\Masters\Documents\글`.
- Local executable: `C:\Users\Masters\Documents\글\dist\WriterWorkbench\WriterWorkbench.exe`.
- HTML reference: `C:\Users\Masters\Downloads\writing-studio_14.html`.
- LLL research docs under `docs/research/`.
- Current WPF relationship-map implementation and tests.

Main audit finding:
- A relationship-map MVP skeleton exists in production code from `4a73ec8`.
- The latest post-complaint commit `332dbbd` changed only `docs/pingpong.txt` and `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`; it changed no production code.
- Therefore that pass did not change user-visible product behavior.
- The reference files existed, but no reference-to-implementation acceptance matrix existed before product-level claims were made.
- This is a NULL-evidence failure: missing operands were treated as if they proved the result.

Reference gap summary:
- Present/partial: relationship surface, character list, relationship list, Canvas nodes, relationship labels, drag-end position persistence, deleting entity removes relationships/layout.
- Missing or not proven: live line redraw during drag, arrowheads, curved/reverse-link handling, self-relationship rejection, duplicate same-direction rejection, direct double-click node/label editing, local executable UI proof after the latest claim.

Commands/evidence used:
- `git log --oneline --decorate --max-count=8`
- `git show --numstat --oneline 4a73ec8`
- `git show --numstat --oneline 332dbbd`
- `rg` checks against `writing-studio_14.html`, `MainWindow.xaml`, `MainWindow.xaml.cs`, `StoryStructureStore.cs`, and relationship-map tests.
- Local executable metadata check.

Large manuscript regression check:
- Not touched in this audit; this was documentation/evidence work only.

Remaining problems:
- Do not call Relationship Map "working" at user/reference level until the reference gap matrix is converted into acceptance criteria and verified.
- Next implementation should address validation and interaction gaps before additional visual polish.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. This audit added a report document only.

## 2026-06-25 KST feature implementation update: V2 customization profile core

Feature(s) implemented:
- Added a Workbench customization profile model for editable command placements, shortcuts, and macros.
- Added JSON profile storage at `settings/workbench-profiles.json`.
- Added validation for unknown command ids, duplicate UI slots, duplicate shortcut bindings, and duplicate macro ids.
- Added a resolver for ordered command placements and macro execution steps.
- Added a default profile factory that converts the existing command catalog slots into editable profile data instead of keeping the toolbar as hardcoded-only UI furniture.

Files changed:
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfile.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileStore.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationResolver.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileTests.cs`
- `docs/pingpong.txt`

Tests added:
- Profile roundtrips arbitrary command placements, shortcuts, macros, parameters, and Korean text.
- Profile supports many custom slots without the old MVP slot limit.
- Store rejects unknown commands, duplicate slots, and shortcut conflicts.
- Resolver returns ordered placements for a surface/area and macro steps.
- ProjectPaths exposes the V2 customization profile path under `settings/`.
- Default profile factory converts command catalog slots into editable profile data.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkbenchCustomizationProfileTests` -> 6 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 130 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- `EditorBox_TextChanged`, document body save, preview render, export, snapshot, and relationship-map drawing paths were not connected to this profile store.

Remaining problems:
- The runtime UI still needs to load this profile and render toolbar/panel/shortcut assignments from it.
- Macro execution is represented as data and resolver output; actual macro runner is still next-stage work.
- Window preset/profile composition still needs to be joined so monitor layout, tool slots, shortcuts, and graphics presets can be applied as one workspace profile.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary JSON profile data.

## 2026-06-27 KST feature implementation update: Detached HTML workbench surfaces and duplicate guard

Feature(s) implemented:
- Detached workbench windows can now host the HTML workbench for `작품 수정`, `메인`, `미리보기`, and `관계도` surfaces when opened from MainWindow.
- Detached HTML workbench messages reuse the same MainWindow bridge for commands, active scene edits, binder commands, relationship map edits, trash restore, shortcut edits, and remote settings.
- MainWindow now sends HTML state to both the main WebView and detached HTML workbench windows.
- MainWindow HTML views now claim their actual logical surface: editor, preview, relationship-map, or general HTML workbench.
- Duplicate surface guard now prevents MainWindow from switching HTML view when a detached workbench already owns the target surface.
- Detached windows refresh surface button availability when MainWindow changes surfaces, another detached window claims a surface, or a detached window closes.
- Local executable refreshed at `dist/WriterWorkbench-html-workbench/WriterWorkbench.exe`.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchDetachedWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Detached workbench HTML surfaces use the embedded HTML host and map editor/preview/main to the right active view.
- MainWindow HTML editor view claims the editor surface so detached windows cannot duplicate it.
- MainWindow does not switch HTML active view when a detached window owns the requested target surface.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DetachedWorkbenchHtmlSurfacesUseEmbeddedHtmlHost|MainWindowHtmlEditorViewClaimsEditorSurfaceForDetachedDuplicateGuard"` -> failed before implementation with missing constructor/properties, then passed after implementation.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowDoesNotSwitchHtmlViewWhenDetachedSurfaceOwnsTarget"` -> failed before duplicate guard fix because `_htmlActiveView` changed to `preview`, then passed after claim-first change.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WorkbenchDetachedWindowTests|WorkbenchSurfaceClaimRegistryTests|MainWindowClaimedEditorSurfaceDisablesEditorInDetachedWorkbench|MainWindowHtmlEditorViewClaimsEditorSurfaceForDetachedDuplicateGuard|MainWindowDoesNotSwitchHtmlViewWhenDetachedSurfaceOwnsTarget|MainWindowHtmlViewCommandsStayInsideHtmlWorkbench"` -> 11 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowSmokeTests|WorkbenchDetachedWindowTests|WorkbenchSurfaceClaimRegistryTests|WebWorkbenchPayloadFactoryTests|WebWorkbenchAssetTests"` -> failed once due the changed private `ShowHtmlWorkbenchSurface` reflection signature, then passed 51 tests after restoring a no-argument wrapper.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 200 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~Large|FullyQualifiedName~EditorInputPerformancePolicy|FullyQualifiedName~EditorLargeTextSmoke"` -> 19 passed.
- `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8'))"` -> passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 200 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~Large|FullyQualifiedName~EditorInputPerformancePolicy|FullyQualifiedName~EditorLargeTextSmoke"` -> 19 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not functionally changed.
- HTML state push now avoids creating the main WebView payload when the main HTML WebView is not initialized.
- Large/editor guardrail filter passed 19 tests.

Remaining problems:
- Detached HTML WebView was verified by unit/smoke structure tests, not by a live screenshot/browser interaction pass.
- Detached relationship map now uses the HTML host when opened from MainWindow, but the older WPF relationship map fallback remains for constructor paths without HTML delegates.
- Surface presets still need to place multiple detached windows on monitors automatically.
- Detached window HTML editing shares active scene state through MainWindow; true independent per-window document editing is not implemented.

Separate data artifact 여부:
- 별도 샘플/fixture 프로젝트 데이터 산출물 없음.
- Local executable artifact generated at `dist/WriterWorkbench-html-workbench/WriterWorkbench.exe`.

## 2026-06-27 KST feature implementation update: HTML workbench relationship/trash/shortcut restoration pass

Feature(s) implemented:
- HTML relationship map now supports entity update/delete and relationship update/delete, not just add/drag.
- Relationship edit state reuses the existing left/right forms and marks the edited list item.
- HTML relationship delete actions ask for browser confirmation before posting to the local bridge.
- Deleted scenes can be listed from project `trash/` and restored back into the binder from the HTML reference panel.
- ProjectStore now exposes trash list/restore APIs and restores document JSON/TXT, metadata, manifest entry, and search index.
- HTML shortcut rows now have editable key fields and a save button; keydown capture formats common Ctrl/Alt/Shift gestures.
- Settings rail now exposes practical shortcuts to relationship map, shortcut settings, remote editor, and help.
- Reference rail now shows project/current-scene references plus trash restore entries.
- Published local test executable to `dist/WriterWorkbench-html-workbench/WriterWorkbench.exe`.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Trash restore recreates document files, metadata, manifest entry, and search index.
- HTML payload carries trash items for the reference panel.
- HTML asset tests cover relationship edit/delete messages, trash restore, and shortcut update/capture wiring.
- MainWindow smoke tests cover HTML story update/delete bridge methods.
- MainWindow smoke test covers HTML shortcut update persistence.

Commands run:
- `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8'))"` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~ProjectStoreTests|FullyQualifiedName~WebWorkbenchAssetTests|FullyQualifiedName~WebWorkbenchPayloadFactoryTests|FullyQualifiedName~MainWindowSmokeTests"` -> failed once on trash restore file lock, then passed 58 tests after stream disposal fix.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 197 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~Large|FullyQualifiedName~EditorInputPerformancePolicy|FullyQualifiedName~EditorLargeTextSmoke"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.
- Public repo `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8'))"` -> passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors after nullable test assertion cleanup.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 197 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save behavior was kept as a guardrail only.
- This pass did not add export, trash restore, relationship save, or shortcut save work into the editor input loop.
- Large/editor guardrail filter passed 19 tests.

Remaining problems:
- HTML relationship map is still functional MVP styling, not final visual design.
- Trash restore currently appends restored scenes to the binder instead of restoring the original binder position.
- Shortcut editing supports typed/captured gestures, but no explicit "clear shortcut" UI yet.
- Settings book and reference surfaces are still shallow work surfaces; they now respond, but they are not a full settings/reference system.
- No live visual/browser screenshot pass was run in this heartbeat.

Separate data artifact 여부:
- 별도 샘플/fixture 프로젝트 데이터 산출물 없음.
- Local executable artifact generated at `dist/WriterWorkbench-html-workbench/WriterWorkbench.exe`.

## 2026-06-27 KST feature repair update: HTML metrics, trash delete, relationship map MVP, reference usage

Feature(s) implemented:
- HTML editor visible character counts now refresh on a 1-second debounce while typing.
- The HTML editor still sends body/title changes through the existing WPF bridge; export/snapshot/index work remains outside the typing loop.
- Fixed the binder action bar layout so the `새 장면 / 이름 / 복제 / 삭제 / 위 / 아래` button area stays in a fixed row set instead of growing with binder refreshes.
- Scene delete now stages the removed scene under `trash/{scene-id}-{timestamp}/` before removing it from the binder and search index.
- HTML relationship map is no longer a placeholder: it now has character add controls, relationship add controls, lists, a canvas, relationship lines/labels, draggable nodes, and layout save messages.
- `WebWorkbenchPayload` now carries story entities/relationships/layout data for the HTML relationship surface.
- MainWindow WebView bridge now handles `story.entity.add`, `story.relationship.add`, and `story.layout.update`.
- Added a reference usage report showing how LLL notes and the attached `writing-studio_14.html` were used without copying code/UI/data.

Files changed:
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/reports/reference-usage-2026-06-27.md`
- `docs/pingpong.txt`

Tests added/updated:
- Delete document moves files to project trash and removes manifest/search entries.
- HTML shell contains relationship map controls and canvas anchors.
- HTML script wires 1-second local metric refresh and story map bridge messages.
- HTML CSS locks binder action bar rows and provides relationship map layout styles.
- Payload carries story structure data for HTML relationship map.
- MainWindow HTML story command handlers persist entities, relationships, and node layout.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DeleteDocumentMovesFilesToTrashAndRemovesManifestEntryAndSearchIndex|WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate|WebWorkbenchCssKeepsInWindowRemoteSecondary|PayloadCarriesStoryStructureForHtmlRelationshipMap|MainWindowHtmlStoryCommandsPersistRelationshipMapData"` -> failed first because trash/story payload APIs did not exist, then 6 passed.
- `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8')); console.log('app.js syntax ok')"` -> passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 193 passed.

Large manuscript regression check, if touched:
- The editor input bridge is touched only for visible HTML character count refresh and still uses a debounce.
- The 1-second character count reads the current visible editor text only and does not trigger export, snapshot, full index rebuild, or full project scan.
- Relationship map state reads only story JSON files and does not read manuscript bodies.

Remaining problems:
- Relationship map is MVP only: no edit/delete controls in the HTML relationship surface yet.
- Settings book and reference/resource panels still need real full work surfaces.
- Shortcut editing is still mostly read/list in the HTML shell; full capture/save is still native/old path.
- Trash restore UI is not implemented yet; deleted scene files are recoverable from disk under `trash/`.
- This does not claim that all removed HTML-era functions are restored.

Separate data artifact 여부:
- `docs/reports/reference-usage-2026-06-27.md` was added as a handoff artifact.

## 2026-06-27 KST feature implementation update: HTML remote editor and shortcut list bridge

Feature(s) implemented:
- HTML workbench payload now includes catalog commands and shortcut bindings for HTML-side settings screens.
- HTML remote-control editor can add, remove, and reorder remote commands, then save explicitly through the WebView bridge.
- WPF bridge now handles `remoteSettings.update` and persists the remote command order into the active customization profile.
- HTML shortcut screen now renders shortcut bindings with search/filter.
- Dist publish updated `dist/WriterWorkbench-menu-remote/WriterWorkbench.exe` and copied current `WebWorkbench` assets.

Files changed:
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Payload exposes available catalog commands and shortcut bindings for HTML settings.
- MainWindow applies HTML remote settings updates to the customization profile and preserves order.
- HTML asset tests cover remote settings, shortcut search, and WebView bridge anchors.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CreatesDashboardPayloadWithActiveEditorTextOnly|PayloadExposesCatalogCommandsForHtmlRemoteSettingsEvenWhenToolbarProfileIsSparse|MainWindowAppliesHtmlRemoteSettingsUpdateToCustomizationProfile|WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate"` -> 5 passed.
- `node -e "new Function(require('fs').readFileSync('src/WriterWorkbench/WebWorkbench/app.js','utf8')); console.log('app.js syntax ok')"` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate"` -> 2 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 188 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-menu-remote --no-restore` -> failed because restore assets for `net8.0-windows/win-x64` were missing.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-menu-remote` -> passed.
- `node -e "new Function(require('fs').readFileSync('dist/WriterWorkbench-menu-remote/WebWorkbench/app.js','utf8')); console.log('dist app.js syntax ok')"` -> passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- Export, autosave, and editor input loops do not run remote settings save; remote settings persist only on explicit HTML save.

Remaining problems:
- Shortcut screen is searchable/read-only in this pass; actual shortcut editing/save still needs a dedicated HTML shortcut editor bridge.
- Relationship map remains an HTML shell entry point unless the native relationship map is ported into the HTML workbench.
- Some older WPF surfaces remain as host/legacy code while the HTML workbench becomes the main UI.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-27 KST feature implementation update: HTML surface promoted to main

Feature(s) implemented:
- Promoted the HTML/WebView workbench surface to the user-facing `메인` surface.
- Removed the visible `HTML 작업대` entry from native top menu, static toolbar, default command slots, and default top-menu profile placements.
- `view.main.open` now opens the HTML main surface.
- Legacy restored `main` surface values now normalize to the HTML main surface.
- Legacy `view.htmlWorkbench.open` ids are no longer registered as visible commands; persisted old profile/widget command ids are normalized to `view.main.open`.
- Detached workbench `메인` selection now points at the HTML main surface id so it cannot be treated as a separate native main surface.
- Web fallback/sample text now calls the surface `메인`.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Application/StartupSurfaceResolver.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileStore.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/Core/Workspace/WorkbenchSurfaceCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/StartupSurfaceResolverTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchSurfaceClaimRegistryTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Default command registry no longer exposes legacy `view.htmlWorkbench.open`.
- Default command slots expose `메인` and `작품 수정`, not `HTML 작업대`.
- Default top menu profile does not include `view.htmlWorkbench.open`.
- `메인` command opens the HTML main surface.
- Legacy restored `main` surface opens the HTML main surface.
- Startup surface resolver maps legacy `main` to `html-workbench`.
- Legacy profile/widget ids normalize from `view.htmlWorkbench.open` to `view.main.open`.
- Surface availability treats `html-workbench` as `메인` and does not expose old native `main`.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "AppCommandCatalogTests|DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlMain|MainWindowContainsWorkspacePresetCommands|MainWindowMainCommandOpensHtmlMainSurface|MainWindowRestoresLegacyMainSurfaceAsHtmlMainSurface"` -> failed before implementation, then the expanded focused suite passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "AppCommandCatalogTests|DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlMain|ProfileStoreNormalizesLegacyHtmlWorkbenchCommandToMainCommand|WebWorkbenchPayloadFactoryTests|MainWindowContainsWorkspacePresetCommands|MainWindowMainCommandOpensHtmlMainSurface|MainWindowRestoresLegacyMainSurfaceAsHtmlMainSurface|LegacyMainSurfaceNormalizesToHtmlWorkbenchSurface|WorkbenchSurfaceClaimRegistryTests"` -> 14 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 185 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release --self-contained false -o dist\WriterWorkbench-menu-remote` -> succeeded.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 185 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> first parallel run hit a DLL file lock while tests were compiling, then rerun alone with 0 warnings, 0 errors.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not changed.
- This change only routes surface/menu/profile command ids and does not add export/save work to input loops.

Remaining problems:
- The old native `MainSurface` still exists internally as a legacy/private WPF panel but is no longer a user-facing default route.
- The HTML main surface still needs the next UI pass for draggable remote edit controls and better visual polish.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary profile/widget data.

## 2026-06-27 KST feature implementation update: HTML-only UI boundary pass 1

Feature(s) implemented:
- Added an explicit HTML active view state to the WebWorkbench payload.
- Added HTML panels for editor, preview, relationship map, shortcuts, remote settings, and help.
- `view.editor.open`, `view.preview.toggle`, `story.relationshipMap.open`, `shortcuts.openSettings`, `remote.openSettings`, and `help.open` now stay inside the HTML shell instead of opening WPF surfaces/windows.
- WPF `EditorSurface`, `PreviewSurface`, and `RelationshipMapSurface` are no longer used by user command routing; they remain legacy/private surfaces for now.
- Preview text is generated by the local engine and sent to HTML instead of switching to WPF PreviewSurface.
- Focus mode now keeps the HTML editor surface as the visible UI while WPF still handles Windows fullscreen/topmost behavior.
- HTML asset tests now require mode panels and JS view switching.

Files changed:
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Editor command stays inside the HTML main surface.
- Preview, relationship map, and help commands keep HTML visible and WPF surfaces collapsed.
- WebWorkbench payload carries `ActiveView` and `PreviewText`.
- HTML asset tests assert the new mode panels and `setActiveView`/`renderPreview` script paths.
- Existing relationship-map command test now treats WPF relationship map canvas as legacy while the user-facing command opens the HTML relationship view.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowEditorCommandStaysInsideHtmlMainSurface|MainWindowViewCommandsStayInsideHtmlShellBoundary"` -> failed before implementation, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CreatesDashboardPayloadWithActiveEditorTextOnly|WebWorkbenchHtmlContainsFullWorkbenchShellAnchors|WebWorkbenchScriptWiresRemoteDragTabsAndEditorUpdate"` -> failed before payload/view-panel implementation, then passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 186 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release --self-contained false -o dist\WriterWorkbench-menu-remote` -> succeeded.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 186 passed.

Large manuscript regression check, if touched:
- Large manuscript input/save loops were not changed.
- HTML preview text is derived when state is pushed, not on every browser keypress.
- Editor input still sends debounced `activeScene.update` messages.

Remaining problems:
- Relationship map, shortcut editing, remote editing, and help are now HTML-owned shells, but their full editors still need to be implemented in HTML.
- Legacy WPF surfaces/windows still exist in code for compatibility and tests, but user command routing no longer opens them.
- Detached workbench still needs a separate HTML host strategy for fully custom detached surfaces.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary data.

## 2026-06-27 KST bugfix implementation update: HTML return, remote dedupe, detached topmost

Feature(s) implemented:
- Added `view.editor.open` / `작품 수정` command so the HTML workbench can return to the native editor surface.
- Added `작품 수정` to the native view menu and HTML top menu placement.
- HTML workbench payload now guarantees the editor-return command even when a saved widget menu exists.
- HTML workbench hides the native WPF remote layer and remote toggle/show no longer opens a second native remote while HTML is visible.
- Detached workbench and legacy detached document windows now start topmost; newly detached windows are activated after show so the last detached window rises to the top layer.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml`
- `src/WriterWorkbench/DetachedDocumentWindow.xaml`
- `tests/WriterWorkbench.Tests/AppCommandCatalogTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchDetachedWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Command catalog registers `view.editor.open` as `작품 수정`.
- Default HTML top menu includes the editor-return command.
- HTML workbench can return to the editor surface and restore native chrome.
- HTML workbench remote toggle does not open a second native remote layer.
- Detached workbench window starts as a topmost layer.
- HTML payload preserves saved widget menu entries while appending the required editor-return command if missing.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CreatesDefaultCommandRegistry|DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlWorkbench|MainWindowCanReturnFromHtmlWorkbenchToEditorSurface|MainWindowHtmlWorkbenchDoesNotOpenSecondNativeRemoteControlLayer|DetachedWorkbenchWindowStartsAsTopmostLayer"` -> failed before implementation, then 5 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter PayloadUsesWidgetRegistryForMenuAndRemoteWhenProvided` -> failed before payload menu guard, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "CreatesDefaultCommandRegistry|DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlWorkbench|MainWindowCanReturnFromHtmlWorkbenchToEditorSurface|MainWindowHtmlWorkbenchDoesNotOpenSecondNativeRemoteControlLayer|DetachedWorkbenchWindowStartsAsTopmostLayer|PayloadUsesWidgetRegistryForMenuAndRemoteWhenProvided"` -> 6 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 179 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -r win-x64 --self-contained false -o dist\WriterWorkbench-menu-remote --no-restore` -> failed because restored assets did not include `net8.0-windows/win-x64`.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release --self-contained false -o dist\WriterWorkbench-menu-remote` -> succeeded.
- Public repo: `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo: `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 179 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- This change only touches command routing, HTML menu payload, remote layer visibility, and detached window z-order.

Remaining problems:
- Detached `작품 수정`, `메인`, and `미리보기` still assign a surface role rather than hosting full native surfaces.
- HTML workbench visual design is still an MVP shell and needs a dedicated redesign pass.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-26 KST reference lock update: LLL structure to Writer Workbench shell

Feature(s) implemented:
- Added a reference-lock document that converts LLL local structure observations into Writer Workbench implementation constraints.
- Locked the direction that HTML/WebView2 should become the primary workbench surface, while WPF remains the host for files, windows, shortcuts, and native commands.
- Defined non-copy boundaries: no LLL executable analysis, no asset/code copy, no user data extraction, no WebView cache/Login Data access, and no LLL format compatibility.
- Defined the next implementation scope around settings separation, widget registry, command-rendered menu/remote, and path restoration.

Files changed:
- `docs/reference-locks/lll-structure-to-workbench.md`
- `docs/pingpong.txt`

Tests added:
- None. This is a design/reference-lock update only.

Commands run:
- Local structure inspection for `C:\Users\Masters\AppData\Local\Muvel`
- Local structure inspection for `C:\Users\Masters\AppData\Local\com.muvel`
- `git log --oneline -5`
- `git status --short --branch`

Large manuscript regression check, if touched:
- Large manuscript code paths were not touched.
- The reference lock explicitly keeps export, snapshot, full index rebuild, and full manuscript scan out of the input loop.

Remaining problems:
- The HTML workbench still needs to be rebuilt from this reference lock.
- Settings are not yet split into the locked `settings/*.json` stores.
- Widget registry is not yet the renderer source for menu/remote UI.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Only a reference-lock document was added.

## 2026-06-26 KST feature implementation update: LLL-inspired settings and HTML workbench shell slice

Feature(s) implemented:
- Added separated project settings paths under `settings/`.
- Added `ProjectAppSettings` and `ProjectSettingsStore` for `settings/app.json`.
- Added `WorkbenchWidgetRegistry`, `WidgetInstance`, and `WidgetRegistryStore` for `settings/widget-registry.json`.
- MainWindow project initialization now creates/loads `app.json` and `widget-registry.json`.
- Default customization profile now includes top menu placements for project/manuscript/story/view/tools/help groups.
- Web workbench payload now separates toolbar, top menu, and remote commands.
- Web workbench payload now uses `widget-registry` instances for menu/remote rendering when provided.
- HTML workbench shell was rebuilt around top menu, left rail, center surface, right inspector, floating remote, and bottom status anchors.
- Floating remote in the HTML shell supports icon-only/title mode at the web layer.

Files changed:
- `docs/superpowers/plans/2026-06-26-lll-workbench-shell.md`
- `docs/pingpong.txt`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/AppSettings/ProjectAppSettings.cs`
- `src/WriterWorkbench/Core/AppSettings/ProjectSettingsStore.cs`
- `src/WriterWorkbench/Core/AppSettings/WorkbenchWidgetRegistry.cs`
- `src/WriterWorkbench/Core/AppSettings/WidgetRegistryStore.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/ProjectSettingsStoreTests.cs`
- `tests/WriterWorkbench.Tests/WidgetRegistryStoreTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`

Tests added:
- Project paths expose separated `settings/*.json` files.
- Missing `settings/app.json` creates Korean-safe default JSON.
- Existing `settings/app.json` loads without overwriting user values.
- Missing `settings/widget-registry.json` creates default widget instances from placements.
- Existing widget registry loads without replacing user order.
- Payload separates top menu and remote commands.
- Payload uses widget registry for menu/remote when provided.
- Default profile includes top menu placements.
- HTML asset contains full workbench shell anchors.
- MainWindow initialization creates separated settings files.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter ProjectSettingsStoreTests` -> failed before store existed, then 3 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WidgetRegistryStoreTests` -> failed before registry store existed, then 2 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchPayloadFactoryTests` -> failed before payload fields existed, then passed after implementation.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlWorkbench` -> failed before menu placements existed, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchHtmlContainsFullWorkbenchShellAnchors` -> failed before shell anchors existed, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowInitializationCreatesSeparatedProjectSettingsFiles` -> failed before MainWindow wiring existed, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ProjectSettingsStoreTests|WidgetRegistryStoreTests|WebWorkbenchPayloadFactoryTests|WebWorkbenchAssetTests|DefaultProfileFactoryIncludesTopMenuPlacementsForHtmlWorkbench|MainWindowInitializationCreatesSeparatedProjectSettingsFiles"` -> 12 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 173 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> succeeded.

Large manuscript regression check, if touched:
- Editor input loop was not touched.
- Export, snapshot, full index rebuild, and full manuscript scan were not added to `EditorBox_TextChanged`.
- New settings/widget files are created during project initialization, not while typing.

Remaining problems:
- HTML workbench is now structurally closer to the target shell, but the embedded editor is still metadata/dashboard-level and not the final writing surface.
- Widget registry can drive menu/remote rendering data, but user-facing widget editing UI is not implemented yet.
- Path restorer is still represented by session state plus `app.json`; separate `path-restorer.json` is not implemented yet.
- WebView visual validation with an actual running app screenshot is still needed after the next UI polish pass.

Separate data artifact 여부:
- 별도 샘플 프로젝트/fixture 산출물 없음. Tests create only temporary settings JSON data.

## 2026-06-27 KST bugfix update: HTML workbench startup surface actually visible

Bug fixed:
- The new HTML workbench shell existed in the published files, but startup restored the global session `Surface = editor`, so the user still saw the old WPF editor and it looked like nothing changed.
- Project `settings/app.json` already had `LastSurface = html-workbench`, but MainWindow did not use that project-level value to choose the startup surface.
- Opening the HTML surface during tests could block on WebView2 initialization, so HTML startup needed to show the surface first and initialize WebView asynchronously.

Feature(s) implemented:
- Added `StartupSurfaceResolver` to apply project-level startup surface and last scene from `settings/app.json`.
- Changed empty/invalid session fallback from native editor to HTML workbench.
- MainWindow now applies project app settings to startup session state after loading `settings/app.json`.
- MainWindow now persists last surface/scene back into project `settings/app.json`.
- HTML surface switching now shows the surface immediately and initializes WebView2 in the background when the window is loaded.

Files changed:
- `src/WriterWorkbench/Core/Application/AppSessionState.cs`
- `src/WriterWorkbench/Core/Application/AppSessionStateService.cs`
- `src/WriterWorkbench/Core/Application/StartupSurfaceResolver.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/AppSessionStateServiceTests.cs`
- `tests/WriterWorkbench.Tests/StartupSurfaceResolverTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Project `LastSurface = html-workbench` overrides legacy global `Surface = editor`.
- Project `LastSurface = editor` is still preserved once the user explicitly saves that state.
- Invalid session surface now falls back to HTML workbench.
- MainWindow initialization now asserts `HtmlWorkbenchSurface` is visible and `settings/app.json` keeps `html-workbench`.

Commands run:
- Inspected `%LOCALAPPDATA%\WriterWorkbench\app.session.json` -> found `Surface = editor`.
- Inspected `C:\WriterWorkbench\Projects\Sample.writerproj\settings\app.json` -> found `LastSurface = html-workbench`.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "StartupSurfaceResolverTests|NormalizesInvalidGraphicPresetToDefault|FallsBackToEmptyStateWhenFileIsInvalid"` -> failed before resolver existed, then 4 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowInitializationCreatesSeparatedProjectSettingsFiles|MainWindowCanShowHtmlWorkbenchSurface|AppSessionStateServiceTests|StartupSurfaceResolverTests"` -> initially timed out on WebView2 blocking, then 8 passed after async initialization fix.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 175 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> succeeded.
- Reset local runtime state for `C:\WriterWorkbench\Projects\Sample.writerproj\settings\app.json` and `%LOCALAPPDATA%\WriterWorkbench\app.session.json` to `html-workbench` after an earlier run had persisted `editor`.
- Re-ran `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Re-ran `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 175 passed.
- Re-ran `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> succeeded.
- Found a second runtime issue: legacy/native `editor` could still be persisted as startup state. Added persisted startup-surface mapping so `editor` is saved as `html-workbench`.
- Added `StartupSurfaceResolver.ToPersistedStartupSurface` test.
- Re-ran `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "StartupSurfaceResolverTests|MainWindowInitializationCreatesSeparatedProjectSettingsFiles"` -> 4 passed.
- Re-ran `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Re-ran `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 176 passed.
- Re-ran publish, relaunched `dist\WriterWorkbench-menu-remote\WriterWorkbench.exe`, then confirmed both `settings/app.json` and `%LOCALAPPDATA%\WriterWorkbench\app.session.json` keep `Surface/LastSurface = html-workbench`.

Large manuscript regression check, if touched:
- Editor input loop was not touched.
- Startup surface resolution and WebView initialization do not add text scanning or export/snapshot work to typing.

Remaining problems:
- The HTML workbench is now the actual startup surface when project settings say so, but the center surface still needs the real writing editor bridge rather than metadata-only display.
- The user-facing widget editor is still not implemented.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary session/settings JSON data.

## 2026-06-25 KST feature implementation update: Icon menu and customizable remote control bar

Feature(s) implemented:
- Replaced the top fixed command strip with a Photoshop-like top menu row.
- Added icon badges to top-level menu categories: 파일, 편집, 원고, 구조, 보기, 창, 도구, 도움말.
- Added a visible `리모콘` shortcut bar under the menu row.
- The remote control bar renders from customization profile placements at `remote/main`.
- Default profiles now include remote placements for 저장, 새 장면, 관계도, 전체 TXT, 창 분리, 스냅샷.
- Added `리모콘 편집`, a visible settings window for choosing remote commands, labels, and order.
- Saving remote settings updates the active customization profile and redraws the remote bar immediately.
- Existing stateful controls for 미리보기, 집중, 자동저장, 창 프리셋, 그래픽 프리셋 remain visible in the option row.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/RemoteControlSettingsWindow.xaml`
- `src/WriterWorkbench/RemoteControlSettingsWindow.xaml.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/RemoteControlSettingsWindowTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Default customization profile includes editable `remote/main` placements.
- MainWindow contains the icon menu bar and remote control bar.
- MainWindow renders remote control buttons from profile order and custom labels.
- RemoteControlSettingsWindow applies checked rows into `remote/main` placements while preserving other placements.
- RemoteControlSettingsWindow marks existing remote placements with their saved order and label.
- Existing command-surface smoke test now recognizes menu items as command hosts, not only buttons.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~WorkbenchCustomizationProfileTests|FullyQualifiedName~MainWindowSmokeTests.MainWindowContainsIconMenuAndRemoteControlBar"` -> failed before remote/menu implementation.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "FullyQualifiedName~MainWindowSmokeTests.MainWindowRendersRemoteControlBarFromCustomizationProfile|FullyQualifiedName~MainWindowSmokeTests.MainWindowContainsIconMenuAndRemoteControlBar|FullyQualifiedName~WorkbenchCustomizationProfileTests.DefaultProfileFactoryIncludesCustomizableRemoteControlPlacements"` -> passed after implementation.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter RemoteControlSettingsWindowTests` -> failed before the edit window existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RemoteControlSettingsWindowTests|MainWindowContainsIconMenuAndRemoteControlBar"` -> 3 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 146 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/save paths were not touched.
- Full smoke suite still includes the 80k append guard test.

Remaining problems:
- 하위 메뉴 항목에는 아직 개별 아이콘을 다 붙이지 않았고, 이번 범위는 메뉴 카테고리 아이콘 중심.
- 기존 메인 화면 command grid와 상단 리모콘이 일부 명령을 함께 노출할 수 있음.
- 리모콘 편집은 MVP라 드래그 정렬/검색/그룹 프리셋은 아직 없음.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST feature update: Resizable remote layer and display mode

Feature(s) implemented:
- Remote control layer is now resizable with `ResizeMode=CanResizeWithGrip`.
- Added a visible resize grip named `RemoteLayerResizeGrip`.
- Added display mode switching on the remote layer.
- The layer can switch between `아이콘+제목` and `아이콘만`.
- Icon-only mode hides command labels and narrows command buttons.
- Icon+title mode restores labels and wider command buttons.

Files changed:
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml`
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/RemoteControlLayerWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- RemoteControlLayerWindow requires `ResizeMode.CanResizeWithGrip`.
- RemoteControlLayerWindow requires `RemoteLayerResizeGrip`.
- RemoteControlLayerWindow can switch between `RemoteControlDisplayMode.IconAndTitle` and `RemoteControlDisplayMode.IconOnly`.
- Icon-only mode collapses title labels and narrows command buttons.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter RemoteControlLayerWindowTests` -> failed before display mode APIs existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowRendersTopmostMovableRemoteControlLayerFromCustomizationProfile|MainWindowContainsIconMenuAndRemoteControlEntryPoint"` -> 2 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 149 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/save paths were not touched.
- Full smoke suite still includes the 80k append guard test.

Remaining problems:
- Remote layer size and display mode are not persisted yet.
- Remote layer position is not persisted yet.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST feature update: Topmost movable remote control layer

Feature(s) implemented:
- Replaced the in-app fixed remote panel with a separate `RemoteControlLayerWindow`.
- Remote control is now a true floating layer, not bounded by the main app window.
- The layer uses `Topmost=True`, `ShowInTaskbar=False`, `WindowStyle=None`, and `ResizeMode=NoResize`.
- The layer has a visible drag handle named `RemoteLayerDragHandle`; users can drag it away from the app window.
- Tests verify that negative `Left`/`Top` coordinates are accepted, so the layer is not clamped to the main window.
- Added `remote.show` / `리모콘 표시` so the layer can be shown/recentered again from the Tools menu.
- Remote buttons still render from customization profile placements at `remote/main`.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml`
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/RemoteControlLayerWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- RemoteControlLayerWindow is topmost, not shown in taskbar, frameless, no-resize, and has a draggable handle.
- RemoteControlLayerWindow renders `remote/main` placements in order.
- RemoteControlLayerWindow raises command requests when buttons are clicked.
- MainWindow renders a topmost movable remote-control layer from customization profile data.
- MainWindow exposes both `리모콘 표시` and `리모콘 편집` commands.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RemoteControlLayerWindowTests|MainWindowRendersTopmostMovableRemoteControlLayerFromCustomizationProfile|MainWindowContainsIconMenuAndRemoteControlEntryPoint"` -> failed before `RemoteControlLayerWindow` and `remote.show` existed, then passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 148 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/save paths were not touched.
- Full smoke suite still includes the 80k append guard test.

Remaining problems:
- Remote layer position is not persisted yet.
- Layer opacity, side snapping, and monitor-specific preset placement are not implemented yet.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST bugfix update: Remote control visible in-app floating panel

Problem:
- The detached OS-window remote control could be hidden or not obvious in real use.
- Screenshot verification showed no visible remote control in the main app surface.

Fix:
- Removed the separate OS-window remote control approach from the runtime path.
- Added an always-visible in-app floating panel named `RemoteControlFloatingPanel` on the right side of MainWindow.
- The panel is separate from the top menu/option row, matching a website-style side floating widget.
- Remote buttons still render from customization profile placements at `remote/main`.
- The panel includes a visible `편집` button for remote shortcut customization.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/RemoteControlPaletteWindow.xaml` removed
- `src/WriterWorkbench/RemoteControlPaletteWindow.xaml.cs` removed
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/RemoteControlPaletteWindowTests.cs` removed
- `docs/pingpong.txt`

Tests added/updated:
- MainWindow now requires `RemoteControlFloatingPanel` and `RemoteFloatingButtonPanel` to exist.
- Remote control rendering test now checks the in-app floating panel, not a separate OS window.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowRendersFloatingRemoteControlPanelFromCustomizationProfile|MainWindowContainsIconMenuAndRemoteControlEntryPoint"` -> failed before panel existed, then passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 146 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/save paths were not touched.
- Full smoke suite still includes the 80k append guard test.

Remaining problems:
- Floating panel position is fixed to the right side inside MainWindow; drag/pin/opacity settings are not implemented yet.
- It may cover a small part of the right inspector until dock/side preference is added.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST feature implementation update: Detached floating remote control palette

Feature(s) implemented:
- Removed the inline remote-control shortcut strip from the main top option row.
- Added `RemoteControlPaletteWindow`, a separate small tool-window palette for remote shortcuts.
- MainWindow now owns the palette at runtime, shows it after project startup, and keeps it beside the main window.
- The palette follows main-window move, resize, and minimize/restore state.
- Remote palette buttons still render from customization profile placements at `remote/main`.
- The palette includes a `편집` button that opens the existing remote-control editor.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/RemoteControlPaletteWindow.xaml`
- `src/WriterWorkbench/RemoteControlPaletteWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/RemoteControlPaletteWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- MainWindow no longer exposes an inline `RemoteControlBar`.
- MainWindow renders a floating `RemoteControlPaletteWindow` from profile order and custom labels.
- RemoteControlPaletteWindow renders profile placements as vertical command buttons.
- RemoteControlPaletteWindow raises command requests when shortcut buttons are clicked.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "RemoteControlPaletteWindowTests|MainWindowRendersFloatingRemoteControlPaletteFromCustomizationProfile|MainWindowContainsIconMenuAndRemoteControlEntryPoint"` -> failed before palette class existed, then passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 148 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input/save paths were not touched.
- Full smoke suite still includes the 80k append guard test.

Remaining problems:
- Floating palette position is automatic beside the main window; manual docking side/offset persistence is not implemented yet.
- Palette drag-to-pin and opacity settings are not implemented yet.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST feature implementation update: Profile-driven main command grid

Feature(s) implemented:
- Main screen quick command grid now has a named `MainCommandGrid` surface.
- MainWindow renders that grid from the active customization profile's `toolbar/main` placements.
- Profile order, labels, and command ids are used to create WPF command buttons.
- Non-main placements are ignored by this surface, so panel/context-menu assignments remain separate.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added:
- MainWindow renders the main command grid from a customization profile, preserving placement order and labels.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowRendersMainCommandGridFromCustomizationProfile` -> failed before the render method existed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowRendersMainCommandGridFromCustomizationProfile` -> 1 passed after implementation.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 134 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- This stage only rebuilds the main screen command button grid from profile metadata and does not read manuscript bodies.

Remaining problems:
- Top toolbar is still static XAML and should be migrated after the main command grid path proves stable.
- The profile editor UI does not exist yet; profiles can be created/edited through JSON and tests only.
- Shortcut manager and macro runner still need to consume the V2 profile.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary in-memory profile data.

## 2026-06-25 KST feature implementation update: Detached workbench slot selection and duplicate surface blocking

Feature(s) implemented:
- Added a workbench surface catalog for `작품 수정`, `메인`, `미리보기`, and `관계도`.
- Added a surface claim registry so one surface role cannot be assigned to multiple windows at the same time.
- Added a blank detached workbench window where the user chooses which workbench surface that window should become.
- Detached window disables surfaces already used by the main window or another detached window.
- Detached window releases its claimed surface when closed.
- The existing `창 분리` command now opens the blank detached workbench selector instead of immediately opening another document editor window.
- MainWindow now claims its current visible surface; if main is on `작품 수정`, detached workbench cannot switch to `작품 수정`.

Files changed:
- `src/WriterWorkbench/Core/Workspace/WorkbenchSurfaceCatalog.cs`
- `src/WriterWorkbench/Core/Workspace/WorkbenchSurfaceClaimRegistry.cs`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/WorkbenchSurfaceClaimRegistryTests.cs`
- `tests/WriterWorkbench.Tests/WorkbenchDetachedWindowTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added:
- Main surface claim blocks a detached duplicate until main moves away.
- A detached owner can move between surfaces without leaving stale duplicate claims.
- Availability marks surfaces occupied by other owners.
- Detached workbench starts blank and disables the surface used by main.
- Detached workbench claims a selected surface and blocks duplicate selection.
- MainWindow claimed editor surface disables editor selection in detached workbench.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkbenchSurfaceClaimRegistryTests` -> failed before registry existed, then 3 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkbenchDetachedWindowTests` -> failed before detached workbench window existed, then 2 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowClaimedEditorSurfaceDisablesEditorInDetachedWorkbench` -> failed before MainWindow had a surface registry, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WorkbenchSurfaceClaimRegistryTests|WorkbenchDetachedWindowTests|MainWindowClaimedEditorSurfaceDisablesEditorInDetachedWorkbench"` -> 6 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 140 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- This stage changes window/surface assignment only and does not connect to `EditorBox_TextChanged` or document body reads.

Remaining problems:
- Detached workbench currently assigns the surface role and displays the selected role; it does not yet host the full editor/preview/main/relationship-map UI.
- Existing detached document editor window still exists in code but is no longer used by the `창 분리` command.
- Help text still describes the old document-detach behavior and should be updated after the mojibake/help text cleanup pass.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary in-memory/window state.

## 2026-06-26 KST feature update: Custom focus duration

Feature(s) implemented:
- Added a visible focus duration input next to the focus button.
- Focus duration defaults to 40 minutes and supports 1-240 minutes.
- Starting focus mode now uses the entered duration instead of a hard-coded 40 minutes.
- Focus button idle label updates to the selected duration.
- Focus duration is saved in app session state and restored on startup.

Files changed:
- `src/WriterWorkbench/Core/Application/AppSessionState.cs`
- `src/WriterWorkbench/Core/Application/AppSessionStateService.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/AppSessionStateServiceTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Main window exposes the focus duration input with default `40`.
- Focus mode starts with a custom `25` minute duration.
- App session state persists focus duration.
- App session state clamps focus duration into the supported range.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowContainsCustomFocusDurationControl|MainWindowStartsFocusWithCustomDurationMinutes|NormalizesFocusDurationIntoSupportedRange|SavesAndLoadsLastWorkspaceState"` -> failed before implementation with missing focus duration input, then passed 4 tests.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 157 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 157 passed.

Large manuscript regression check, if touched:
- Editor input, save, binder, and large manuscript paths were not touched.
- Focus duration changes only affect focus-mode startup and app session state.

Remaining problems:
- Focus early-exit confirmation length is still fixed at 20 characters.
- Main command grid labels from customization profile still use stored labels until the profile editor updates them.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-26 KST feature update: Binder right-click scene action menu

Feature(s) implemented:
- Added a right-click context menu to the binder scene list.
- Binder right-click menu actions: rename scene, duplicate scene, delete scene, move up, move down, create current-scene snapshot, export current scene TXT, detach window.
- Right-clicking a binder item now selects/focuses that scene first, so existing scene commands target the clicked row.
- Menu actions reuse the existing command handler path instead of duplicating delete/rename/move logic.

Files changed:
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Main window smoke test now verifies the binder right-click menu command tags.
- Test also verifies binder items have a right-click selection event setter.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter MainWindowBinderHasRightClickSceneActionMenu` -> failed before implementation because binder context menu was missing, then passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 154 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 154 passed.

Large manuscript regression check, if touched:
- Editor input, document save, and large manuscript paths were not touched.
- Binder menu uses existing metadata/manifest-centered scene command paths.

Remaining problems:
- Context menu item enable/disable state is not dynamic yet.
- Context menu does not show keyboard shortcut hints yet.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-26 KST feature/bugfix update: Remote toggle, shortcut capture, autosave mirror

Feature(s) implemented:
- Added `remote.toggle` / `리모콘 켜기/끄기` so the floating remote layer can be hidden and shown again.
- Kept existing `remote.show` / `리모콘 표시` for explicit recenter/show behavior.
- Added default `Ctrl+Alt+R` binding for remote toggle in the legacy shortcut profile and V2 customization profile defaults.
- Shortcut settings now captures real key gestures in the shortcut cell instead of accepting arbitrary typed text like `dsadsada`.
- Shortcut settings rejects invalid shortcut text at save time.
- Autosave now writes a separate JSON/TXT mirror under `autosaves/` before the canonical document save.

Files changed:
- `src/WriterWorkbench/Core/Commands/AppCommandIds.cs`
- `src/WriterWorkbench/Core/Commands/AppCommandCatalog.cs`
- `src/WriterWorkbench/Core/Commands/ShortcutProfileService.cs`
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileFactory.cs`
- `src/WriterWorkbench/Core/Storage/ProjectPaths.cs`
- `src/WriterWorkbench/Core/Storage/ProjectStore.cs`
- `src/WriterWorkbench/MainWindow.xaml`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/ShortcutSettingsWindow.xaml`
- `src/WriterWorkbench/ShortcutSettingsWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/ProjectStoreTests.cs`
- `tests/WriterWorkbench.Tests/ShortcutSettingsWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Remote toggle shows and hides the floating layer.
- Main menu exposes both remote show and remote toggle commands.
- Shortcut settings captures `Ctrl+Alt+S` from a key event and ignores bare raw letters.
- Shortcut settings rejects invalid typed shortcut text.
- Autosave mirror writes separate UTF-8 JSON/TXT files without mutating the manifest.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ShortcutSettingsWindowTests|MainWindowRemoteControlToggleShowsAndHidesLayer|SaveAutosaveCopyWritesSeparateJsonAndTxtWithoutMutatingManifest"` -> failed before implementation with missing APIs, then passed 6 tests.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 153 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo fresh clone `dotnet restore WriterWorkbench.sln` -> restored assets for new checkout.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 153 passed.

Large manuscript regression check, if touched:
- Editor input loop was not touched.
- Autosave adds one extra atomic JSON/TXT mirror write only when autosave runs, not on `EditorBox_TextChanged`.

Remaining problems:
- Autosave mirror restore UI is not implemented yet; files are recoverable from disk.
- Existing projects with old invalid shortcut strings may still load them until the user opens shortcut settings and saves corrected rows.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST UI tuning update: Remote layer default size and icon-only alignment

Feature(s) implemented:
- Set the floating remote layer default size to `260 x 450`, aligned to the user's screenshot target in 10px units.
- Icon-only remote mode now uses 40px buttons and keeps them top-left aligned.
- Icon-and-title mode keeps remote buttons stretched across the panel width.

Files changed:
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml`
- `src/WriterWorkbench/RemoteControlLayerWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/RemoteControlLayerWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Remote layer default width/height assertion.
- Remote icon-only mode width and left-alignment assertion.
- Remote icon-and-title mode stretch-alignment assertion.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter RemoteControlLayerWindowTests` -> failed before implementation with expected size/button width mismatches, then passed 3 tests.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 149 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-menu-remote --no-restore` -> published local executable.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 149 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors after rerunning build separately.

Large manuscript regression check, if touched:
- Large manuscript paths were not touched.
- This change only adjusts the floating remote layer size and display-mode alignment.

Remaining problems:
- Remote layer position, size, and display mode are not persisted yet.
- Remote command editing is still a placeholder entry point.

Separate data artifact 여부:
- 별도 데이터 산출물 없음.

## 2026-06-25 KST feature implementation update: Detached relationship map host

Feature(s) implemented:
- Added `WorkbenchRelationshipMapView`, a reusable relationship-map view for detached workbench windows.
- Detached workbench `관계도` selection now loads story entities, relationships, and saved node layout from `StoryStructureStore`.
- Detached relationship map displays entity list, relationship list, Canvas nodes, relationship lines, and relationship labels.
- Detached relationship-map nodes can be dragged; drag release saves node layout through `StoryStructureStore.SaveNodeLayoutAsync`.
- MainWindow now passes its active story structure store into detached workbench windows.
- Cleaned the new workbench surface catalog labels to proper Korean text.

Files changed:
- `src/WriterWorkbench/Core/Workspace/WorkbenchSurfaceCatalog.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml`
- `src/WriterWorkbench/WorkbenchDetachedWindow.xaml.cs`
- `src/WriterWorkbench/WorkbenchRelationshipMapView.xaml`
- `src/WriterWorkbench/WorkbenchRelationshipMapView.xaml.cs`
- `tests/WriterWorkbench.Tests/WorkbenchDetachedWindowTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Detached relationship map surface loads story data into entity list, relationship list, and Canvas.
- Detached window async relationship-map loading uses the UI Dispatcher without deadlocking the STA test.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter DetachedRelationshipMapSurfaceLoadsStoryDataIntoCanvas` -> failed before relationship-map host APIs existed, then passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WorkbenchDetachedWindowTests|WorkbenchSurfaceClaimRegistryTests|MainWindowClaimedEditorSurfaceDisablesEditorInDetachedWorkbench"` -> 7 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 141 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- Relationship-map loading reads only story structure JSON files and does not read manuscript bodies.

Remaining problems:
- Detached relationship map currently supports viewing and node-position drag/save; add/update/delete editors still live in the main relationship-map surface.
- Detached `작품 수정`, `메인`, and `미리보기` still show the role assignment screen and need real hosted surfaces.
- Existing old `DetachedDocumentWindow` remains in code but is not used by the current `창 분리` command.

Separate data artifact 여부:
- 별도 데이터 산출물 없음. Tests create only temporary story JSON data.

## 2026-06-25 KST feature implementation update: V2 customization profile bootstrap

Feature(s) implemented:
- Added a customization profile service that loads the active workbench profile for a project.
- Missing `settings/workbench-profiles.json` now creates a default profile instead of leaving customization as an in-memory idea.
- Existing user profiles are loaded without being replaced by the default profile.
- A requested profile id can be selected when multiple profiles exist.
- MainWindow now wires the customization profile service into project configuration and startup initialization.

Files changed:
- `src/WriterWorkbench/Core/Customization/WorkbenchCustomizationProfileService.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/WorkbenchCustomizationProfileServiceTests.cs`
- `docs/pingpong.txt`

Tests added:
- Missing profile file creates a default profile under project settings.
- Existing user profile is loaded without being replaced by default data.
- Requested profile id wins when multiple profiles exist.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkbenchCustomizationProfileServiceTests` -> failed before the service existed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkbenchCustomizationProfileServiceTests` -> 3 passed after implementation.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WorkbenchCustomizationProfileServiceTests|WorkbenchCustomizationProfileTests|MainWindowConstructsOnStaThread"` -> 10 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- This stage only adds profile bootstrap during project startup/configuration and does not connect to `EditorBox_TextChanged`.

Remaining problems:
- Toolbar/panel rendering still needs to consume the active profile instead of using static XAML buttons.
- Shortcut settings still use the old shortcut profile service; it needs to merge into the V2 workbench profile.
- Macro definitions are still data only; execution is not wired yet.

## 2026-06-27 KST feature implementation update: Workspace presets restore detached workbench windows

Feature(s) implemented:
- Workspace presets now store detached workbench window surface assignments and placements.
- Preset save captures each detached workbench window once by surface id.
- Preset apply closes the previous detached workbench set and recreates the saved detached surfaces.
- Detached preset restore keeps stored coordinates, including off-screen or multi-monitor coordinates, instead of clamping them.
- Legacy preset JSON without detached-window data loads with an empty detached-window list.

Files changed:
- `src/WriterWorkbench/Core/Workspace/WorkspaceDetachedWindowPlacement.cs`
- `src/WriterWorkbench/Core/Workspace/WorkspacePreset.cs`
- `src/WriterWorkbench/Core/Workspace/WorkspacePresetService.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `tests/WriterWorkbench.Tests/WorkspacePresetTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Workspace preset stores detached workbench window placements.
- Workspace preset persists detached workbench windows to disk.
- Legacy preset without detached-window field loads as an empty list.
- MainWindow captures detached workbench windows into a preset.
- MainWindow applies detached workbench windows from a preset.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WorkspacePresetTests` -> failed before implementation, then 7 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "MainWindowCapturesDetachedWorkbenchWindowsInPreset|MainWindowAppliesDetachedWorkbenchWindowsFromPreset"` -> failed before implementation, then 2 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 205 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 205 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.

Large manuscript regression check, if touched:
- Large manuscript input/render/save paths were not touched.
- Guardrail tests `Large|Performance|Stress` passed: 19 tests.

Remaining problems:
- Preset monitor identity matching is still coordinate-based; if monitors are re-ordered, the user must save/apply an appropriate preset.
- Presets do not yet persist every remote-control layer visual setting in the same bundle.
- Detached HTML views still depend on WebView2 availability at runtime.

## 2026-06-27 KST bugfix update: HTML workbench character count no longer jumps between full scene and editor segment

Root cause:
- HTML workbench rendered full-scene metadata counts from the backend.
- After local typing, `updateActiveEditorMetrics` overwrote the same count fields using only the textarea text.
- Large scenes are edited through a bounded segment, so the local textarea count could be far smaller than the full scene count.

Feature(s) implemented:
- HTML workbench now stores the backend full-scene counts plus the original visible editor-segment counts.
- Local typing updates use a full-scene delta: full count - original visible segment + current visible segment.
- Small non-segment scenes still update directly from the editor text.
- JS local counting now mirrors the backend paragraph parser by splitting blank-line paragraphs, trimming, and counting paragraph text only.

Files changed:
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Added regression coverage that local metric updates keep full-scene delta logic for segment mode and no longer write raw textarea length into full-scene count fields.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchLocalMetricUpdateUsesFullSceneDeltaForSegmentMode` -> failed before implementation, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 5 passed.
- `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 206 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.
- Public repo `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 5 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 206 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.

Large manuscript regression check, if touched:
- Large manuscript editor behavior was indirectly touched through HTML local metric display.
- Guardrail tests `Large|Performance|Stress` passed: 19 tests.

Remaining problems:
- The WPF inspector still has separate labels for current / whitespace-excluded / whitespace-included counts; wording may need cleanup so the user always knows which basis is shown.
- HTML local metric updates are still approximate until save when paragraph boundaries are changed heavily inside a segment, but they no longer collapse to the visible segment total.

## 2026-06-27 KST feature implementation update: Character Count Consistency Pass

Feature(s) implemented:
- Added shared plain-text metric calculation for editor text using the same paragraph split/trim rules as document merge.
- WPF scene inspector now separates count labels as `편집 구간`, `전체 공백 제외`, and `전체 공백 포함`.
- WPF bottom status now labels its document count as `전체 공백 포함`.
- HTML workbench normalizes active-scene count labels at runtime to `전체 공백 제외` and `전체 공백 포함`.
- HTML binder row metadata now marks scene counts as `공백 제외` so the number's basis is visible.

Files changed:
- `src/WriterWorkbench/Core/Documents/DocumentMetricsService.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `tests/WriterWorkbench.Tests/DocumentMetricsServiceTests.cs`
- `tests/WriterWorkbench.Tests/MainWindowSmokeTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- Plain text metrics use the same paragraph rules as the editor merge path.
- MainWindow runtime scene inspector exposes explicit character-count labels.
- HTML workbench script exposes explicit character-count labels and binder count basis.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DocumentMetricsServiceTests|MainWindowUsesExplicitCharacterCountLabels|WebWorkbenchUsesExplicitCharacterCountLabels"` -> failed before implementation, then 4 passed.
- `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 6 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 209 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.
- Public repo `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchLocalMetricUpdateKeepsFullSceneCountsStable` -> 1 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 6 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 209 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- Public repo `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DocumentMetricsServiceTests|MainWindowUsesExplicitCharacterCountLabels|WebWorkbenchUsesExplicitCharacterCountLabels"` -> 4 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 6 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 209 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.

Large manuscript regression check, if touched:
- Large manuscript editor input path was not changed.
- Count display wording and local editor-segment measurement were touched.
- Guardrail tests `Large|Performance|Stress` passed: 19 tests.

Remaining problems:
- Static XAML/HTML still contain some legacy mojibake labels; runtime normalization fixes the count labels, but broader UI text cleanup remains.
- HTML local metric updates remain approximate until save when paragraph boundaries change heavily inside a segment.

Separate data artifact:
- 별도 데이터 산출물 없음. Tests create only temporary data.

## 2026-06-28 KST bugfix update: Large manuscript text visibility

Bug fixed:
- Large scene editor only displayed the bounded editable segment and did not put the remaining manuscript text on screen.
- Root cause: `DocumentEditorTextView` only carried `Text`, `IsSegmentMode`, and `VisibleParagraphCount`; the hidden remainder stayed in storage but was absent from the HTML workbench payload.
- For a manuscript editing program, retained-but-invisible body text is a product bug, not an acceptable performance optimization.

Feature(s) implemented:
- `DocumentEditorTextView` now carries `RemainderText` and `TotalParagraphCount`.
- `WebWorkbenchScene` payload now carries `RemainderText` and `TotalParagraphCount`.
- HTML workbench editor now shows a separate `전체 원고 보기` panel for the remaining large-scene text.
- The bounded textarea remains for responsive segment editing, while the rest of the manuscript is visible below it as read-only text.

Files changed:
- `src/WriterWorkbench/Core/Documents/DocumentEditorTextView.cs`
- `src/WriterWorkbench/Core/Documents/DocumentEditorTextService.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayload.cs`
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchPayloadFactory.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/DocumentEditorTextServiceTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchPayloadFactoryTests.cs`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- `ExposesRemainingLargeDocumentTextForOnScreenReading`
- `PayloadCarriesRemainingLargeDocumentTextForOnScreenReading`
- `WebWorkbenchShowsRemainingLargeManuscriptText`

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExposesRemainingLargeDocumentTextForOnScreenReading|PayloadCarriesRemainingLargeDocumentTextForOnScreenReading|WebWorkbenchShowsRemainingLargeManuscriptText"` -> failed before implementation, then 3 passed.
- `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DocumentEditorTextServiceTests|WebWorkbenchPayloadFactoryTests|WebWorkbenchAssetTests"` -> 21 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 214 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 22 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable and WebWorkbench assets.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "ExposesRemainingLargeDocumentTextForOnScreenReading|PayloadCarriesRemainingLargeDocumentTextForOnScreenReading|WebWorkbenchShowsRemainingLargeManuscriptText"` -> 3 passed.
- Public repo `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "DocumentEditorTextServiceTests|WebWorkbenchPayloadFactoryTests|WebWorkbenchAssetTests"` -> 21 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 214 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 22 passed.

Large manuscript regression check, if touched:
- Large manuscript display path was touched.
- Bounded edit segment remains small for typing responsiveness.
- Remaining manuscript text is now visible in the workbench payload/UI.
- Guardrail tests `Large|Performance|Stress` passed: 22 tests.

Remaining problems:
- The remaining text panel is read-only. Next required step is segment navigation/editing so a user can move the editable window to later paragraphs without switching to a separate preview.

Separate data artifact:
- 별도 데이터 산출물 없음. Tests create only temporary data.

## 2026-06-28 KST bugfix update: WebWorkbench asset cache busting

Bug fixed:
- User reported the HTML character count fix still showed the same behavior.
- Investigation found the code path was fixed in source/dist, but WebView2 was still loading `index.html` from a stable file URI and `index.html` loaded `app.js` / `styles.css` from stable URLs.
- This allowed stale HTML/JS assets to remain plausible even after publish.

Feature(s) implemented:
- Added `WebWorkbenchAssetVersion.CreateIndexUri(indexPath)`.
- `MainWindow` now navigates WebView2 to `index.html?wwv={asset-version}`.
- `index.html` now propagates the same `wwv` value into `styles.css?wwv=...` and `app.js?wwv=...`.
- Dist publish now contains cache-busted HTML asset loading.

Files changed:
- `src/WriterWorkbench/Core/WebWorkbench/WebWorkbenchAssetVersion.cs`
- `src/WriterWorkbench/MainWindow.xaml.cs`
- `src/WriterWorkbench/WebWorkbench/index.html`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- `WebWorkbenchIndexUriUsesAssetVersionForCacheBusting`
- `WebWorkbenchHtmlPropagatesAssetVersionToStylesAndScript`

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchIndexUriUsesAssetVersionForCacheBusting` -> failed before implementation, then 1 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchHtmlPropagatesAssetVersionToStylesAndScript` -> failed before implementation, then included in 2-test cache-busting pass.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchIndexUriUsesAssetVersionForCacheBusting|WebWorkbenchHtmlPropagatesAssetVersionToStylesAndScript"` -> 2 passed.
- `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 8 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 211 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable and WebWorkbench assets.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "WebWorkbenchIndexUriUsesAssetVersionForCacheBusting|WebWorkbenchHtmlPropagatesAssetVersionToStylesAndScript"` -> 2 passed.
- Public repo `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 8 passed.
- Public repo `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 211 passed.
- Public repo `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.

Large manuscript regression check, if touched:
- Large manuscript logic was not changed.
- Guardrail tests `Large|Performance|Stress` passed: 19 tests.

Remaining problems:
- If the user starts an older executable from another folder, that older build can still show stale behavior. The tested current executable is under `dist/WriterWorkbench-html-workbench/WriterWorkbench.exe`.

Separate data artifact:
- 별도 데이터 산출물 없음. Tests create only temporary data.

## 2026-06-27 KST bugfix update: Stable HTML character count display

Bug fixed:
- HTML workbench full-scene character counts could alternate between saved full metadata and tiny visible-editor counts.
- Root cause: `updateActiveEditorMetrics()` wrote local textarea metrics back into the same `active-length` / `active-length-spaces` elements used for full-scene metadata.
- Large scenes only load an editable segment into the textarea, so this made full counts appear as values such as 7/9 after the 1 second local update.

Feature(s) implemented:
- Full scene counts now remain metadata-backed and stable in `전체 공백 제외` / `전체 공백 포함`.
- Local 1 second metric refresh now writes only to a separate `active-editor-metrics` line.
- Large scene editor shows segment-local counts as `편집 구간 · 공백 제외 ... · 공백 포함 ...`.

Files changed:
- `src/WriterWorkbench/WebWorkbench/index.html`
- `src/WriterWorkbench/WebWorkbench/app.js`
- `src/WriterWorkbench/WebWorkbench/styles.css`
- `tests/WriterWorkbench.Tests/WebWorkbenchAssetTests.cs`
- `docs/pingpong.txt`

Tests added/updated:
- `WebWorkbenchLocalMetricUpdateKeepsFullSceneCountsStable` confirms local metric refresh does not write to full-scene count elements.

Commands run:
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchLocalMetricUpdateKeepsFullSceneCountsStable` -> failed before implementation, then 1 passed.
- `node --check src\WriterWorkbench\WebWorkbench\app.js` -> passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter WebWorkbenchAssetTests` -> 6 passed.
- `dotnet build WriterWorkbench.sln --no-restore` -> 0 warnings, 0 errors.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore` -> 209 passed.
- `dotnet test tests\WriterWorkbench.Tests\WriterWorkbench.Tests.csproj --no-restore --filter "Large|Performance|Stress"` -> 19 passed.
- `dotnet publish src\WriterWorkbench\WriterWorkbench.csproj -c Release -o dist\WriterWorkbench-html-workbench --no-restore` -> published local executable.

Large manuscript regression check, if touched:
- Large document input/storage path was not changed.
- Large scene display path was touched to keep full metadata counts stable while showing segment-local counts separately.
- Guardrail tests `Large|Performance|Stress` passed: 19 tests.

Remaining problems:
- This fixes the count-display conflict. It does not yet add full-document live recount before save for hidden large-scene paragraphs.

Separate data artifact:
- 별도 데이터 산출물 없음. Tests create only temporary data.
